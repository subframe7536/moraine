# Implementation Plans

Generated by the improve skill on 2026-09-08. Execute in the order below unless dependencies say otherwise. Each executor must read the plan fully before starting, honor its STOP conditions, and update the row when done.

## Execution order and status

| Plan | Title                                                                     | Priority | Effort | Depends on | Status |
| ---- | ------------------------------------------------------------------------- | -------- | ------ | ---------- | ------ |
| 001  | Replace Design with layered Theme contracts and native control forwarding | P1       | L      | —          | DONE   |

Status values: TODO, IN PROGRESS, DONE, BLOCKED (with a one-line reason), or REJECTED (with a one-line rationale).

## Dependency notes

- Plan 001 is self-contained. Its internal stages are ordered because the public Provider cutover cannot safely support both Design and Theme without the compatibility layer forbidden by the specification.

## Findings considered and rejected

- A multi-plan migration was rejected because the public Provider and all component consumers share one context contract; separate deployable plans would require a dual runtime or leave the branch broken between plans.
- A Design compatibility shim was rejected because the project is pre-1.0 and both specifications explicitly allow breaking changes.
- A replacement runtime slot/Variant registry was rejected because local namespaces and Recipe-declared keys already provide the required type and evaluation information.
- A generic state callback/override layer was rejected because state attributes plus local dynamic style composition cover the current call sites with less API and lower runtime coupling.
- A complete public API golden manifest was rejected because focused declaration/completion checks catch the required regressions without duplicating the source type system.
- A documentation extractor rewrite was rejected unless a focused failing intersection proves a concrete parser gap.
- New dependencies and architecture layers were rejected because existing Solid, Vite, Vitest, TypeScript, and `cn` capabilities cover the migration and verification work.
