## Edge-case parity sweep — reference matrix

This matrix is the **canonical scope** for the edge-case parity sweep tracked in
`todo.md`. It freezes the audit boundary so downstream tasks share the same
definition of what is in-scope.

### Scope

- Existing Moraine components and shared hooks under `src/elements`,
  `src/forms`, `src/navigation`, `src/overlays`, and `src/shared`.
- Each in-scope target is mapped to its closest reference counterpart in
  [`base-ui`](./base-ui) (React, `base-ui/packages/react/src/<name>`) and
  [`kobalte`](./kobalte) (SolidJS, `kobalte/packages/core/src/<name>`).
- "—" means the reference library has no direct counterpart; behavior in that
  cell should be derived from neighboring primitives in the same library, not
  re-implemented from scratch.

### Non-goals (explicitly out of scope)

The following components do **not** exist in Moraine yet and are **out of scope**
for this sweep. Do not introduce them as part of the parity work:

- `ScrollArea` (base-ui has `scroll-area`; kobalte has none — not in Moraine)
- `OTPField` (base-ui has `otp-field` — not in Moraine)
- `Combobox` / `Autocomplete` (base-ui has `combobox` and `autocomplete`,
  kobalte has `combobox` — not in Moraine)
- Color primitives (`color-area`, `color-slider`, `color-wheel`, `color-field`,
  `color-swatch`, `color-channel-field` — kobalte only, not in Moraine)
- Date/time primitives (`time-field`, dates — not in Moraine)
- `Toast`, `Drawer` (Moraine has `Sheet`; `Toast` is not in Moraine)
- `Menubar`, `NavigationMenu`, `Toolbar` (tracked in V1 / Components section of
  `todo.md`, not in this sweep)
- `HoverCard` / `PreviewCard`, `RatingGroup`, `SegmentedControl`, `Skeleton`,
  `Meter`, `Toggle` / `ToggleGroup` (not in Moraine)

Behaviors from these references may still be **studied for inspiration** when
porting edge cases into existing Moraine components, but the components
themselves are not deliverables of this sweep.

### `src/elements`

| Moraine target | base-ui reference | kobalte reference |
| -------------- | ----------------- | ----------------- |
| `accordion`    | `accordion`       | `accordion`       |
| `avatar`       | `avatar`          | `image`           |
| `badge`        | —                 | `badge`           |
| `button`       | `button`          | `button`          |
| `card`         | —                 | —                 |
| `collapsible`  | `collapsible`     | `collapsible`     |
| `icon`         | —                 | —                 |
| `kbd`          | —                 | —                 |
| `progress`     | `progress`        | `progress`        |
| `resizable`    | —                 | —                 |
| `separator`    | `separator`       | `separator`       |

### `src/forms`

| Moraine target                                                    | base-ui reference      | kobalte reference                     |
| ----------------------------------------------------------------- | ---------------------- | ------------------------------------- |
| `checkbox`                                                        | `checkbox`             | `checkbox`                            |
| `checkbox-group`                                                  | `checkbox-group`       | `checkbox` (group via `form-control`) |
| `file-upload`                                                     | —                      | `file-field`                          |
| `form`                                                            | `form`                 | `form-control`                        |
| `form-field`                                                      | `field`, `fieldset`    | `form-control`                        |
| `input`                                                           | `input`                | `text-field`                          |
| `input-number`                                                    | `number-field`         | `number-field`, `spin-button`         |
| `radio-group`                                                     | `radio`, `radio-group` | `radio-group`                         |
| `select` (incl. `base-select`, `multi-select`, `shared/behavior`) | `select`               | `select`, `listbox`                   |
| `slider`                                                          | `slider`               | `slider`                              |
| `switch`                                                          | `switch`               | `switch`                              |
| `textarea`                                                        | `input` (semantics)    | `text-field`                          |

### `src/navigation`

| Moraine target    | base-ui reference                        | kobalte reference                                  |
| ----------------- | ---------------------------------------- | -------------------------------------------------- |
| `breadcrumb`      | —                                        | `breadcrumbs`                                      |
| `command-palette` | `menu` (typeahead/listbox patterns only) | `listbox`, `search` (listbox/search patterns only) |
| `pagination`      | —                                        | `pagination`                                       |
| `sidebar-frame`   | —                                        | —                                                  |
| `stepper`         | —                                        | —                                                  |
| `tabs`            | `tabs`                                   | `tabs`                                             |

### `src/overlays`

| Moraine target    | base-ui reference                                                                               | kobalte reference                                                                                                                                                           |
| ----------------- | ----------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `base/modal.tsx`  | `dialog`, `utils/InternalBackdrop`, `utils/useAnchoredPopupScrollLock`, `utils/useSwipeDismiss` | `dialog`, `dismissable-layer`, `primitives/create-focus-scope`, `primitives/create-hide-outside`, `primitives/create-interact-outside`, `primitives/create-escape-key-down` |
| `base/popper.tsx` | `floating-ui-react`, `utils/useAnchorPositioning`, `utils/usePositioner`                        | `popper`                                                                                                                                                                    |
| `base/menu`       | `menu`                                                                                          | `menu`                                                                                                                                                                      |
| `context-menu`    | `context-menu`                                                                                  | `context-menu`                                                                                                                                                              |
| `dialog`          | `dialog`, `alert-dialog`                                                                        | `dialog`, `alert-dialog`                                                                                                                                                    |
| `dropdown-menu`   | `menu`                                                                                          | `dropdown-menu`, `menu`                                                                                                                                                     |
| `popover`         | `popover`                                                                                       | `popover`                                                                                                                                                                   |
| `popup`           | `floating-ui-react`, `utils/usePositioner`                                                      | `popper`, `dismissable-layer`                                                                                                                                               |
| `sheet`           | `drawer`                                                                                        | `dialog` (no direct sheet; reuse dialog dismissal)                                                                                                                          |
| `tooltip`         | `tooltip`                                                                                       | `tooltip`                                                                                                                                                                   |

### `src/shared` (hooks)

| Moraine hook                           | base-ui reference                                                                              | kobalte reference                                                                       |
| -------------------------------------- | ---------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------- |
| `use-controllable-value`               | (internal `useControlled` patterns across components)                                          | `primitives/create-controllable-signal`                                                 |
| `use-disclosure-state`                 | (internal open-state mappings, `utils/collapsibleOpenStateMapping`, `utils/popupStateMapping`) | `primitives/create-disclosure-state`, `primitives/create-toggle-state`                  |
| `use-event-listener`                   | (component-local listeners)                                                                    | (component-local listeners)                                                             |
| `use-loading-auto`                     | —                                                                                              | —                                                                                       |
| `use-media-query`                      | `unstable-use-media-query`                                                                     | (i18n locale primitives only)                                                           |
| `use-selectable-collection-navigation` | `menu`, `select`, `tabs`, `toolbar` (per-component nav)                                        | `primitives/create-collection`, `primitives/create-dom-collection`, `selection`, `list` |
| `use-transition-presence`              | `utils/getDisabledMountTransitionStyles`, `utils/styles.tsx`                                   | `primitives/create-transition`                                                          |

### Audit results

Classifications use the fixed Base UI `3011fba8f` and Kobalte `2e8ce473` revisions. A plan is complete only when every behavior named by its plan is represented below.

| Plan | Audited behavior | Classification | Upstream evidence and local outcome | Regression evidence |
| ---- | ---------------- | -------------- | ----------------------------------- | ------------------- |
| `useControllableValue` | Uncontrolled initialization snapshots `defaultValue` | ported | Base UI `packages/utils/src/useControlled.ts` initializes state from the first default; Kobalte `packages/core/src/primitives/create-controllable-signal/create-controllable-signal.ts` does the same. Moraine now initializes its local signal once instead of rereading the default after a mode transition. | `src/shared/use-controllable-value.test.ts` — `uses the initial default value for the lifetime of uncontrolled state` |
| `useControllableValue` | Controlled detection and preservation of local state across mode transitions | ported | Kobalte derives controlled mode from `value() !== undefined` and does not replace its local signal while controlled. Moraine now follows that Solid-compatible behavior; Base UI freezes initial React mode and warns, which is not part of Moraine's hook contract. | `src/shared/use-controllable-value.test.ts` — `computes controlled updates without mutating the preserved uncontrolled value`, `stores undefined as an uncontrolled value across mode transitions` |
| `useControllableValue` | Direct and functional updates use the latest exposed value without reactive capture | ported | Base UI accepts `React.SetStateAction`; Kobalte resolves value/updater inputs inside `untrack` against the exposed value. Moraine now accepts a Solid-style updater and resolves consecutive updates against current state. | `src/shared/use-controllable-value.test.ts` — `applies functional updates to the latest uncontrolled value` |
| `useControllableValue` | `Object.is` equality short-circuits publication | ported | Kobalte checks `Object.is(nextValue, value())` before mutating or notifying. Moraine now performs the same check, including stable `NaN`. | `src/shared/use-controllable-value.test.ts` — `does not publish Object.is-equal updates` |
| `useControllableValue` | `undefined` remains a valid local uncontrolled value | ported | Both references treat `undefined` as the controlled-mode boundary, while their local state can still contain `undefined`. Moraine no longer replaces a locally written `undefined` with `defaultValue`. | `src/shared/use-controllable-value.test.ts` — `stores undefined as an uncontrolled value across mode transitions` |
| `useControllableValue` | Option getter evaluation stays single and reactive | ported | Kobalte caches controlled mode/value with memos and reads the default only during signal creation. Moraine now gives each option getter one relevant evaluation rather than rereading it through fallback branches or setters. | `src/shared/use-controllable-value.test.ts` — `evaluates option getters once per relevant change` |
| `useControllableValue` | Change callbacks remain owned by component consumers | intentional-divergence | Kobalte's primitive accepts `onChange`, but Moraine's documented hook tuple has no callback option and all audited consumers invoke their public callbacks after direct setter calls. Adding Kobalte's callback would duplicate notifications and change the local contract, so it is intentionally omitted. | Direct setter-call inventory in `accordion`, `collapsible`, `checkbox`, `input-number`, `radio-group`, `select`, `multi-select`, `switch`, `tabs`, `stepper`, `modal`, `popper`, `menu`, and `dropdown-menu`; all 14 affected suites pass. |
| `useEventListener` | Registration returns symmetric cleanup with the same listener and options | verified | Base UI `packages/utils/src/addEventListener.ts` and its test register once and remove with the same arguments. Moraine's attach helpers already match that lifecycle. | `src/shared/use-event-listener.test.ts` — `registers a listener and removes it on cleanup`, `normalizes object options for cleanup` |
| `useEventListener` | Reactive retargeting and option replacement clean up before re-registration | verified | Kobalte `create-form-reset-listener.ts` and `create-escape-key-down.ts` register inside Solid effects and use owner cleanup on effect replacement. Moraine supports the same owner-scoped composition without retaining old targets or option registrations. | `src/shared/use-event-listener.test.ts` — `detaches the previous target before attaching a replacement`, `re-registers changed options without retaining the previous registration` |
| `useEventListener` | Stable registration dispatches current reactive handler state | verified | Base UI `packages/utils/src/useStableCallback.ts` uses a stable trampoline for latest React callbacks; Kobalte's handlers read reactive props from stable closures. Solid closures give Moraine the same observable result without an extra wrapper or registration. | `src/shared/use-event-listener.test.ts` — `dispatches current reactive state without duplicate registration` |
| `useEventListener` | Boolean, capture, passive, once, and AbortSignal options are preserved | verified | Base UI's typed helper forwards the complete `AddEventListenerOptions` object, including one-shot usage in `usePressAndHold.ts`; Kobalte uses capture, passive, and once options in its local listeners. Moraine forwards the caller's object unchanged through cleanup. | `src/shared/use-event-listener.test.ts` — `forwards advanced options unchanged to registration and cleanup`; existing boolean and object-option tests |
| `useEventListener` | Listener maps register and remove exactly their defined event set | verified | The references use explicit symmetric event sets in overlay and focus primitives. Moraine's map helper skips undefined entries and retains one cleanup for every registered entry. | `src/shared/use-event-listener.test.ts` — `registers all listeners and disposes them together`, `registers and removes only defined map entries` |
| `useEventListener` | Missing/disabled targets, repeated setup, owner cleanup, and server construction | verified | Kobalte guards disabled/server paths before registration; Base UI's helper has no ambient browser reads. Moraine represents a disabled target as `null`/`undefined`, returns a no-op cleanup, and references no `window` or `document` during construction. | `src/shared/use-event-listener.test.ts` — `returns a noop cleanup when target is missing`, `is safe to construct without a browser target`; reactive retargeting and cleanup tests |
| `useEventListener` | Event typing remains local and function-based | intentional-divergence | Base UI additionally accepts listener objects and specializes `AbortSignal`/`VisualViewport`; no current Moraine consumer uses those event surfaces. Expanding the public type contract is outside this sweep, while all supported targets retain concrete event maps and the generic fallback is `Event`, never `any`. | `bun run typecheck`; all affected transition, overlay, select, form-control, and resizable suites pass. |
| `useTransitionPresence` | Initial closed state, reactive entry, data attributes, no element, no motion, and `mode="none"` | verified | Base UI `useTransitionStatus.ts` mounts immediately on open and clears ending state after exit; Kobalte `create-transition.ts` keeps content mounted until `afterExit` and settles zero-duration motion immediately. Moraine already exposes the same supported presence and `data-expanded`/`data-closed` state. | `src/shared/use-transition-presence.test.tsx` — closed construction/reactive opening, `mode is none`, Web Animations no-motion, and computed-style no-motion tests |
| `useTransitionPresence` | Reopen and owner cleanup cancel stale Web Animation completion and fallback timers | verified | Base UI aborts pending completion callbacks and Kobalte clears its timeout when the transition direction changes or owner disposes. Moraine's effect cleanup already prevents stale close completion after reopen. | `src/shared/use-transition-presence.test.tsx` — `cancels a pending close when reopened`, `clears a fallback timer when reopened` |
| `useTransitionPresence` | Element replacement invalidates the previous exit waiter | ported | Both references bind transition work to the current element/ref lifecycle. Moraine's element is now reactive, so replacement reruns the owner effect and cancels the superseded element's promise, listeners, and timer. | `src/shared/use-transition-presence.test.tsx` — `ignores completion from an element that was replaced during exit` |
| `useTransitionPresence` | Canceled Web Animations wait for an active replacement | ported | Base UI `useAnimationsFinished.ts` and its fixed-revision tests re-query `getAnimations()` after rejection and wait while a replacement remains pending/running. Moraine now performs the same recursive recheck instead of treating rejection as successful completion. | `src/shared/use-transition-presence.test.tsx` — `waits for a replacement Web Animation after cancellation` |
| `useTransitionPresence` | Multiple CSS animation names and transition properties settle only after every active motion | ported | Base UI prefers the complete `getAnimations()` set; Kobalte derives transition properties from every changed style key. Moraine's fallback now cycles CSS duration/delay lists across every active name/property, consumes terminal events by `propertyName`/`animationName`, and ignores unrelated or duplicate events instead of decrementing the exit count. | `src/shared/use-transition-presence.test.tsx` — multi-property/name waits plus unrelated/duplicate identity regressions |
| `useTransitionPresence` | Animation/transition cancellation, bubbled descendant events, and computed-duration fallback | ported | Base UI distinguishes cancellation from replacement through rejected animation promises; Kobalte clears timeout-driven transitions on replacement. Moraine now accepts own-target `animationcancel`/`transitioncancel`, continues to ignore descendant events, and retains the maximum-duration timeout when terminal events are absent. | `src/shared/use-transition-presence.test.tsx` — cancellation events, descendant event, and computed-duration fallback tests |
| `useTransitionPresence` | Detached elements cannot retain presence or later settle a newer exit | ported | The references resolve current refs before completion and abort stale work. Moraine now settles an already disconnected exit element immediately and reactive element replacement guards later completion. | `src/shared/use-transition-presence.test.tsx` — `settles immediately when the exit element is already detached`, element replacement test |
| `useTransitionPresence` | Consumer exit-complete ordering | verified | Base UI completes animations before unmount callbacks. Moraine's modal consumer derives `onExitComplete` from both presence accessors becoming false, so it remains after the current overlay/content exit and fires once. | `src/overlays/dialog/dialog.test.tsx` — exit-complete ordering tests; all 14 disclosure, select, and overlay suites pass. |
| `useTransitionPresence` | Real-engine CSS event enumeration, Web Animations replacement timing, and hydration DOM retention | unverified-platform | JSDOM cannot produce layout-driven CSS animations, native `Animation` state, or hydration keys. Fixed upstream source and deterministic mocks prove the guards, but production SSG inspection is pending because the existing docs build fails while importing client-only `solid-toaster` from `docs/routes/components/markdown/toast-hosts.tsx`. | Focused mocked timing tests pass; `bun run docs:preview --host 127.0.0.1` recorded the unrelated server-import failure before a browser could connect. |
| `useMediaQuery` | Server fallback remains stable through initial hydration, then adopts the client snapshot | ported | Base UI `unstable-use-media-query/index.ts` separates the server/default snapshot from the subscribed client snapshot. Moraine now defers the first `matchMedia().matches` write to a microtask and cancels it on disposal, preventing a synchronous `onMount` responsive-tree swap. | `src/shared/use-media-query.test.tsx` — server fallback and disposed-owner tests |
| `useMediaQuery` | Missing `window.matchMedia` preserves the fallback without browser access | ported | Base UI explicitly returns its default snapshot when `matchMedia` is unavailable. Moraine now guards the browser API before constructing a query list. | `src/shared/use-media-query.test.tsx` — `returns the fallback when matchMedia is unavailable` |
| `useMediaQuery` | Modern and legacy MediaQueryList change subscriptions update reactively and clean up symmetrically | ported | Base UI uses `addEventListener('change')`; its supported browsers expose the modern API. The plan also requires the legacy `addListener`/`removeListener` compatibility path, which Moraine now selects only when the modern methods are absent. | `src/shared/use-media-query.test.tsx` — modern event/cleanup and legacy listener tests |
| `useMediaQuery` | Reactive query replacement detaches the old list before subscribing to the new one | ported | Base UI memoizes subscription by query, replacing it when the query string changes. Moraine now accepts either the existing string or an accessor, tracks it in a Solid effect, cancels stale queued snapshots, and retains exactly one active listener. | `src/shared/use-media-query.test.tsx` — `replaces the listener when a reactive query changes` |
| `useMediaQuery` | `@media` prefix normalization and per-owner query-list state | ported | Base UI strips an optional `@media` prefix and constructs a query list per hook instance. Moraine now normalizes the same prefix and keeps all state owner-local without caches. | `src/shared/use-media-query.test.tsx` — `normalizes an @media prefix before creating the query list` |
| `useMediaQuery` | SidebarFrame controlled override remains authoritative | verified | The local contract allows `isMobile` to override the internal media result; the hook changes do not alter that component policy or add Base UI's React options. | `src/navigation/sidebar-frame/sidebar-frame.test.tsx` — 13 tests pass, including `prefers controlled isMobile over internal matchMedia` |
| `useMediaQuery` | Real hydration order across responsive SidebarFrame branches | unverified-platform | JSDOM can prove the fallback survives through the mount microtask but cannot validate hydration keys or breakpoint layout. Production inspection remains blocked by the same existing `solid-toaster` server-import failure recorded above. | Focused microtask test and SidebarFrame suite pass; production SSG failure recorded. |
| `useDisclosureState` | Closed/expanded and disabled data attributes react to state changes | verified | Base UI's `collapsibleOpenStateMapping.ts` exposes mutually exclusive open/closed hooks, while Kobalte's disclosure/toggle primitives preserve reactive open and disabled state. Moraine already updates its documented closed/expanded/disabled attributes consistently. | `src/shared/use-disclosure-state.test.tsx` — `exposes reactive open and disabled data attributes` |
| `useDisclosureState` | Moraine exposes `data-expanded` instead of Base UI's `data-open` | intentional-divergence | Moraine's Accordion/Collapsible contract and UnoCSS selectors use `data-expanded`; replacing it with Base UI's `data-open` would be an API/styling contract change outside this behavior sweep. The mutually exclusive state semantics remain equivalent. | Focused data-attribute test plus Accordion and Collapsible suites |
| `useDisclosureState` | Initial, queued, open-change, zero-height, and empty-content measurement | verified | Base UI `useCollapsiblePanel.ts` measures `scrollHeight` at opening/closing transition boundaries and short-circuits zero-size panels. Moraine already measures immediately on ref assignment and queues a post-insertion/open-change measurement, including zero. | `src/shared/use-disclosure-state.test.tsx` — detached test's initial measurement and `remeasures the current element after open state changes` |
| `useDisclosureState` | Detached and superseded elements cannot publish queued measurements | ported | Base UI resolves the current panel ref in each layout/effect phase and aborts stale close work. Moraine now requires a queued target to remain connected and current; observer replacement also disconnects the prior element. | `src/shared/use-disclosure-state.test.tsx` — detached and superseded-element tests |
| `useDisclosureState` | Reactive content replacement updates Moraine's fixed height variable | ported | Base UI clears its measured dimension back to `auto` after opening, so later content growth remains visible. Moraine intentionally keeps a numeric `--mo-collapsible-content-height`; a per-element `ResizeObserver` is therefore required and now remeasures only the current connected element. | `src/shared/use-disclosure-state.test.tsx` — `remeasures connected content when its size changes` |
| `useDisclosureState` | Owner cleanup releases observation and server construction avoids layout APIs | ported | Base UI tears down ref/effect-owned measurement work; Kobalte primitives are server-safe state only. Moraine now disconnects its observer and clears the current element on cleanup, while constructing without a mounted element or `ResizeObserver` performs no layout access. | `src/shared/use-disclosure-state.test.tsx` — observer cleanup and browser-independent construction tests |
| `useDisclosureState` | Native ResizeObserver scheduling and real scroll geometry | unverified-platform | JSDOM does not implement layout or native ResizeObserver delivery. Deterministic observer/`scrollHeight` mocks prove target ownership and update ordering, but real geometry remains a browser-engine behavior. | Six focused tests and 40 Accordion/Collapsible tests pass. |
| `useLoadingAuto` | Automatic async loading is a Moraine-only API | intentional-divergence | Base UI and Kobalte fixed revisions expose disabled Button behavior but no automatic promise-tracking contract. Moraine retains its documented `loadingAuto` API and uses upstream Button disabled/pending interaction only as context instead of inventing a reference API shape. | `src/shared/use-loading-auto.test.ts`; `src/elements/button/button.test.tsx` |
| `useLoadingAuto` | Plain returns and synchronous throws do not enter loading, and throws propagate | verified | The local contract invokes the public click handler first and only tracks promise-like results when the event remains active. Synchronous errors continue through the caller unchanged. | `src/shared/use-loading-auto.test.ts` — `does not enter loading for synchronous returns or thrown errors` |
| `useLoadingAuto` | Native promises and arbitrary thenables enter and settle loading | verified | Promise assimilation is local behavior with no direct upstream primitive. Moraine already detects callable `then` on objects/functions and normalizes it with `Promise.resolve`. | `src/shared/use-loading-auto.test.ts` — thenable test; Button async lifecycle test |
| `useLoadingAuto` | Overlapping actions remain loading until all completions, regardless of order | ported | A single boolean allowed the first completion to hide a later pending task. Moraine now tracks the owner-local pending count, so out-of-order completion decrements only its own work. | `src/shared/use-loading-auto.test.ts` — `stays loading until every overlapping action settles` |
| `useLoadingAuto` | Rejection clears loading without an unhandled derived rejection or changing the original promise state | ported | The previous ignored `.finally()` chain created a second rejected promise. Moraine now attaches symmetric fulfillment/rejection settlement handlers; the original promise remains rejected for its owner while the tracking chain resolves cleanly. | `src/shared/use-loading-auto.test.ts` — `clears loading on rejection without changing the original rejection` |
| `useLoadingAuto` | Prevented events and disposed owners do not publish automatic state | verified | Prevented clicks never start tracking. Solid disposal removes the hook's memo subscribers, so later completion cannot publish to the disposed owner. | `src/shared/use-loading-auto.test.ts` — prevented-click and disposed-owner tests |
| `useLoadingAuto` | Controlled and automatic loading compose as one disabled state | verified | Moraine combines the explicit flag with pending automatic work using logical OR, so turning controlled loading off does not hide a still-pending action and Button receives one `isLoading` value. | `src/shared/use-loading-auto.test.ts` — controlled/automatic composition test; 51 Button tests pass without new warnings. |
| `useSelectableCollectionNavigation` | Orientation-aware arrows, RTL horizontal direction, Home/End, looping, disabled skipping, empty collections, and legacy `Spacebar` normalization | verified | Base UI `packages/react/src/internals/composite/root/useCompositeRoot.ts` and Kobalte `packages/core/src/tabs/tabs-keyboard-delegate.ts` implement the same directional mapping and enabled-item traversal. Moraine's existing kernel already matches its supported one-dimensional collection contract. | `src/shared/use-selectable-collection-navigation.test.ts` — orientation, RTL, boundary, looping, disabled, empty, and legacy-key groups |
| `useSelectableCollectionNavigation` | Automatic navigation selects then focuses, while manual navigation only focuses | verified | Base UI Tabs activates through focus only when `activateOnFocus` is enabled; Kobalte `create-selectable-collection.ts` separates `setFocusedKey` from `replaceSelection` through `selectOnFocus`. Moraine exposes that distinction as its documented activation mode. | `src/shared/use-selectable-collection-navigation.test.ts` — automatic/manual activation tests; Tabs and Stepper suites |
| `useSelectableCollectionNavigation` | Missing, disabled, or dynamically removed current values recover from the directional boundary | ported | Kobalte's collection handler starts at the first/last enabled key when no focused key exists, and its tabs delegate resolves missing traversal through collection boundaries. Moraine now applies the same recovery when a formerly valid value disappears or becomes disabled instead of leaving navigation stuck. | `src/shared/use-selectable-collection-navigation.test.ts` — `manual mode can focus from an undefined current value`, `recovers from a current value removed from the dynamic collection` |
| `useSelectableCollectionNavigation` | Home/End at the current enabled boundary do not reselect or refocus | ported | Base UI's composite root publishes only when `nextIndex !== highlightedIndex`; Base UI Tabs also guards activation of the active value. Moraine now suppresses redundant callbacks while retaining its existing key-cancellation contract. | `src/shared/use-selectable-collection-navigation.test.ts` — `Home and End are no-ops when already at the enabled boundary` |
| `useSelectableCollectionNavigation` | Recognized navigation keys remain canceled at non-looping or empty boundaries | intentional-divergence | Kobalte prevents the recognized navigation key before resolving a next key, while Base UI prevents only after finding a different index. Moraine retains its established Kobalte-aligned behavior so arrow/Home/End keys do not fall through to page scrolling when a composite cannot move. | Focused boundary, empty, and all-disabled tests; no selection/focus callbacks occur. |
| `useSelectableCollectionNavigation` | Caller-canceled keyboard events are not reinterpreted | ported | Kobalte's event composition exposes `defaultPrevented` to downstream behavior and explicitly preserves it where handlers overlap. The plan requires the shared Moraine kernel to honor cancellation, so it now returns before reading direction or mutating focus/selection. | `src/shared/use-selectable-collection-navigation.test.ts` — `does not handle an event already canceled by the caller` |
| `useSelectableCollectionNavigation` | Enter and Space activation stays limited to owning ARIA patterns | verified | Tabs, RadioGroup, and Stepper call the activation-capable handler on their own tab/radio/button items. Select and Menu use only offset/boundary focus methods; CommandPalette excludes Space and handles Enter itself. Accordion does not consume the hook. This keeps pattern-specific activation outside consumers that do not own it. | Direct consumer inventory; all 10 affected hook/component suites pass (292 tests). |
| `useSelectableCollectionNavigation` | Typeahead remains pattern-specific | intentional-divergence | Base UI Menu/Select and Kobalte `create-type-select.ts` include collection-specific text normalization, repetition, and timeout behavior. Moraine's shared kernel has no text/value contract common to Tabs, RadioGroup, Stepper, Select, Menu, and CommandPalette, so typeahead remains in the owning consumers. | Printable-key hook test plus Select, Menu, and CommandPalette suites |
| `useListVirtualizer` | The TanStack adapter remains a Moraine-owned API | intentional-divergence | Base UI exposes an external-virtualization contract in Combobox rather than a public virtualizer, and Kobalte exposes `virtualized`/`scrollToItem` integration in Listbox and Select. Moraine keeps its existing `virtualRender` adapter and delegates layout to the installed `@tanstack/virtual-core` 3.17.4 instead of copying either upstream API. | Public type tests and List/Select/MultiSelect suites |
| `useListVirtualizer` | Delayed, replaced, nested, and detached scroll elements own exactly one core subscription set | ported | TanStack `Virtualizer._willUpdate()` cleans the old target before observing a replacement, while `_didMount()` returns the owner cleanup. Moraine previously discarded that returned cleanup; it now executes it on disposal and gates retargeting through the current mounted component. Any provided element is used directly, so nested containers require no document-level lookup. | `src/shared/use-list-virtualizer.test.tsx` — cleanup, delayed/replaced target, and pre-microtask disposal tests |
| `useListVirtualizer` | Reactive count, item-key order, estimate changes, and dynamic row measurement invalidate the current layout | ported | TanStack keys measurement caches by item key and requires `measure()` when estimate/order inputs change outside its memoized option identity. Moraine now snapshots keys/estimates once per reactive pass, invalidates changed shared entries, remeasures connected rows, and leaves tail-only count changes to the core count path. | `src/shared/use-list-virtualizer.test.tsx` — reactive estimate and rapid replacement tests; `src/elements/list/list.test.tsx` — measured dynamic rows |
| `useListVirtualizer` | Same-index item replacement refreshes row content without stale render-prop output | ported | Base UI's fixed Combobox virtualized-item tests resolve each displayed item against the current filtered collection/index. Moraine's index-reconciled `For` previously retained JSX created for the old source item; a keyed row snapshot now replaces only rows whose current item/index changes. | `src/shared/use-list-virtualizer.test.tsx` — `rebuilds row identity and content when entries are rapidly replaced` |
| `useListVirtualizer` | Row render props resolve once and mounted rows are measured after final attributes/children | ported | The Solid SSR gate requires a render-prop getter to be resolved once while retaining its component boundary. Moraine now caches the row renderer once; each visible row invokes it once, while reactive style/measurement state updates through row props instead of reinvocation. | `src/shared/use-list-virtualizer.test.tsx` — single getter/invocation test; List dynamic-size test |
| `useListVirtualizer` | Detached virtual rows release element observation | ported | TanStack `measureElement(null)` purges disconnected cached nodes, but Solid ref callbacks do not supply React-style null refs. Moraine now schedules that purge from each keyed row owner cleanup, preventing scroll-window replacement from retaining detached elements. | `src/shared/use-list-virtualizer.test.tsx` — `unobserves detached rows when the virtual window is replaced` |
| `useListVirtualizer` | `scrollToIndex` forwards index/alignment to the current instance and is inert before mount/after disposal | verified | TanStack computes the requested item offset and forwards it through `scrollToFn`. Moraine's stable public callback resolves the current instance at call time, so it cannot retain a replaced or disposed virtualizer. No separate public `scrollToOffset` method is added because the plan preserves the adapter contract. | `src/shared/use-list-virtualizer.test.tsx` — current-instance scrolling test |
| `useListVirtualizer` | Zero items and missing browser observers remain safe | verified | TanStack's default element observer guards an absent `ResizeObserver`, and a zero count produces a zero-sized empty range. Moraine does not construct observer or geometry work until its scroll element attaches. | `src/shared/use-list-virtualizer.test.tsx` — zero items without ResizeObserver and deferred-attachment tests |
| `useListVirtualizer` | Server/client creation order excludes ref-revealed virtual rows until hydration clears | ported | Server rendering has no mounted scroll element, while a client ref can exist during initial node creation. Moraine now exposes the scroll element and client-computed rows only from a guarded post-mount microtask, making the initial tree symmetric and canceling the task on disposal; List's focused test now waits for that documented client phase. | `src/shared/use-list-virtualizer.test.tsx` — hydration-microtask and cancellation tests; 115 focused/dependency tests pass. |
| `useListVirtualizer` | Real browser hydration keys, ResizeObserver delivery, and geometry-driven scroll anchoring | unverified-platform | JSDOM proves single evaluation, deferred creation, cleanup, offset forwarding, and deterministic geometry, but it cannot validate native hydration keys, ResizeObserver scheduling, or layout/scroll anchoring. Production SSG inspection remains blocked by the pre-existing client-only `solid-toaster` server import in `docs/routes/components/markdown/toast-hosts.tsx`. | Focused tests, typecheck, and type tests pass; the recorded `docs:preview` build stops before browser connection. |
| `overlay-modal` | Open, present, overlay/content, and controlled exit states retain resources through exit and cancel stale completion on reopen | verified | Base UI keeps Dialog mounted through transition completion, while Kobalte ties focus/scroll behavior to content presence. Moraine's separate overlay/content presence machines already retain the layer until both surfaces exit; rapid reopening cancels completion and focus restoration. An open root with neither surface acquires no modal resources. | `src/overlays/base/modal.test.tsx` — no-surface and rapid-reopen tests; Dialog exit-order tests |
| `overlay-modal` | Initial focus chooses the first eligible control and Tab loops within the content | ported | Base UI `DialogPopup.tsx` delegates to `FloatingFocusManager`; Kobalte `create-focus-scope.tsx` focuses the first tabbable item and loops at both boundaries. Moraine's existing key trap matched the loop, and its focusable scan now also excludes controls beneath hidden, `aria-hidden`, or inert ancestors. | `src/overlays/base/modal.test.tsx` — initial focus and bidirectional Tab-loop test |
| `overlay-modal` | Programmatic focus escape recovers to the last focused modal descendant | ported | Kobalte's focus scope records `lastFocusedElement` and restores it when focus leaves; Base UI's modal focus manager provides equivalent containment. Moraine previously restarted at the first control, and now retains the last valid descendant for recovery. | `src/overlays/base/modal.test.tsx` — `recovers escaped focus to the last focused element inside the top modal` |
| `overlay-modal` | Close restores focus to the trigger or the previously focused element, skipping removed/disabled targets | ported | Base UI's focus manager supports trigger/previous-element return focus, and Kobalte captures the previously active element before autofocus. Moraine now snapshots the restore target once per layer, restores after exit cleanup, falls back for triggerless open-on-mount, and refuses disconnected, disabled, inert, or hidden targets. | `src/overlays/base/modal.test.tsx` — triggerless, disabled, removed, and nested stack-order restoration tests |
| `overlay-modal` | Only the top layer handles Escape/outside pointer/focus and descendant overlay portals count as inside | verified | Base UI counts nested dialogs and Kobalte's dismissable-layer stack gates every dismissal to the top layer. Moraine's existing stack already provides top-only and descendant interaction semantics; the focus changes no longer make stack registration reactive to trigger replacement. | Modal nested restoration test; Dialog nested Escape/pointer tests; all Popover/Menu/Select competition suites |
| `overlay-modal` | Successful Escape/pointer dismissal prevents default; canceled events and context-menu presses do not dismiss | ported | Kobalte prevents a handled Escape and ignores modal context-menu pointerdown; both upstream systems allow consumer cancellation before dismissal. Moraine now handles document events after target handlers, honors `defaultPrevented`, cancels successful Escape, and ignores secondary/Control-click presses. | `src/overlays/base/modal.test.tsx` — handled Escape, right-click, and caller-cancellation tests |
| `overlay-modal` | Non-dismissible Escape, outside pointer, and focus attempts report prevention without closing | verified | Base UI's `disablePointerDismissal` and Kobalte's cancelable dismissal events keep the layer open. Moraine already prevents Escape/pointer defaults, reports `onClosePrevent`, and recovers escaped focus; affected Dialog/Popup/Sheet tests continue to pass. | Dialog, Popup, and Sheet non-dismissible Escape/pointer suites; modal focus-recovery test |
| `overlay-modal` | Background accessibility isolation is nested, mutation-aware, and descendant-overlay aware | ported | Kobalte `create-hide-outside.ts` reference-counts `aria-hidden`, pauses the previous observer for a nested layer, and preserves top-layer portals. Moraine now provides equivalent owner cleanup, restores original attribute values, hides late body siblings, and consults its overlay stack so a descendant portal remains exposed. | `src/overlays/base/modal.test.tsx` — basic cleanup, nested/late sibling, and descendant portal tests |
| `overlay-modal` | Dialog semantics belong to the content surface | verified | Base UI and Kobalte place `role="dialog"`, `aria-modal`, labelling, and focusability on the popup/content rather than the backdrop. Moraine's `ModalContent` already owns `role="dialog"`, `aria-modal="true"`, ids, descriptions, and `tabIndex=-1`; component plans retain label-specific policy. | Modal focus tests and Dialog semantic/render suites |
| `overlay-modal` | Body scroll locking reference-counts nested owners and restores scrollbar compensation exactly | ported | Base UI `useScrollLock.ts` and Kobalte's dialog scroll prevention both retain the lock until the last modal owner releases it and avoid layout shift. Moraine's shared lock already counted owners; it now adds the viewport scrollbar width to existing body padding on first acquire and restores the exact inline overflow/padding values on final release. | `src/overlays/base/modal.test.tsx` — nested scroll-lock compensation test; Menu scroll-lock consumer tests |
| `overlay-modal` | Trigger and content render values resolve once; closed content is not instantiated | verified | Kobalte's Solid implementation creates Dialog content only within `contentPresent`, and the Moraine SSR gate requires raw render values to be cached once. `ModalTrigger` and `ModalContent` already memoize their render props and guard content construction behind presence. | `src/overlays/base/modal.test.tsx` — getter and closed-content instance tests; Dialog getter-backed test |
| `overlay-modal` | Base UI initial/final-focus and cancelable event-detail props remain internal behavior, not new public props | intentional-divergence | The plan preserves Moraine's `ModalRoot`, `ModalTrigger`, and `ModalContent` contracts. Adding Base UI/Kobalte focus/event callback APIs would expand every consumer; the shared engine instead ports their default observable behavior and keeps Dialog labelling/Sheet gesture APIs in their component plans. | Public typecheck and all 11 affected consumer suites |
| `overlay-modal` | Native touch dismissal heuristics, visual viewport scroll locking, real focus visibility, and portal hydration DOM | unverified-platform | JSDOM cannot reproduce delayed touch click cancellation, iOS/WebKit scroll/pinch behavior, layout-driven scrollbar gutters, or actual portal hydration keys. Deterministic tests cover ownership and event rules; production overlay inspection remains blocked by the existing `solid-toaster` SSG import failure. | 17 modal tests, 280 affected tests, and the full 76-file/1166-test suite pass; existing navigation warning unchanged. |
| `overlay-popper` | Concrete trigger resolution and absolute positioning strategy | ported | Kobalte `popper-root.tsx` computes with `strategy: "absolute"` and `popper-positioner.tsx` renders an absolute wrapper. Moraine already computed absolute coordinates but rendered a fixed wrapper; the wrapper now uses the matching absolute containing-block strategy. | `src/overlays/base/popper.test.tsx` — absolute strategy and trigger-replacement tests |
| `overlay-popper` | Placement, gutter/shift, flip, overflow padding, transform origin, and available-size outputs | ported | Kobalte's pinned Popper applies the same Floating UI offset/flip/shift/size chain and validates string fallback placements before use. Moraine retains its documented default overflow padding of 4, updates placement and transform-origin data reactively, and now rejects invalid fallback lists deterministically. | `src/overlays/base/popper.test.tsx` — reactive placement/transform-origin and invalid-fallback tests; Popover side-placement tests |
| `overlay-popper` | Positioning starts only with connected anchor/floating elements and stops on close, replacement, conditional removal, and owner disposal | ported | Base UI `useAnchorPositioning.ts` clears keep-mounted references while closed; Kobalte scopes `autoUpdate` to the current anchor/positioner effect. Moraine now clears owned refs on conditional disposal, cancels scheduled positioning, marks the wrapper unpositioned/hidden, and ignores stale asynchronous results for detached or replaced nodes. | `src/overlays/base/popper.test.tsx` — removed trigger, replaced trigger, no-surface, and conditional-content cleanup tests |
| `overlay-popper` | Resize, scroll, layout-shift, and ancestor observation use Floating UI ownership | verified | Both references delegate lifecycle tracking to Floating UI `autoUpdate`; Kobalte explicitly disables element resize observation when `ResizeObserver` is unavailable. Moraine continues to delegate its supported concrete-element path and now passes the same availability guard. | `src/overlays/base/popper.test.tsx` — missing-ResizeObserver test; lifecycle cleanup tests |
| `overlay-popper` | Disconnected and zero-size elements do not publish stale positioning state | ported | Base UI discards retained coordinates until the current open is positioned, while Kobalte guards missing references before computing. Moraine now checks current identity, connection, and presence both before and after the asynchronous computation; zero-size geometry remains a valid Floating UI input. | `src/overlays/base/popper.test.tsx` — trigger removal/replacement and default jsdom zero-geometry placement tests |
| `overlay-popper` | Closed lazy content, `forceMount`, exit presence, and rapid reopen preserve correct open-state ownership | ported | Base UI prevents positioning for initially closed keep-mounted content, and Kobalte separates content presence from open state. Moraine now uses `forceMount` only to retain DOM: closed content exposes `data-closed` without observers, dismissal listeners, focus work, scroll locking, or accessibility isolation, while exit/reopen still reuses one content instance. | `src/overlays/base/popper.test.tsx` — lazy content, closed forceMount, rapid reopen, and render-instance tests |
| `overlay-popper` | Content render values are evaluated once across force-mounted state changes | verified | Kobalte's Solid content exists only inside its presence boundary, and Moraine's SSR gate requires raw JSX/render props to be cached before conditional use. `PopperContent` memoizes `contentRender`, so toggling open state does not reread the getter or recreate retained content. | `src/overlays/base/popper.test.tsx` — force-mounted render getter and closed-content instance tests |
| `overlay-popper` | Content z-index propagates to the positioner without overriding Moraine's default stack when content is `auto` | ported | Kobalte copies the computed content z-index to its positioner in a guarded microtask. Moraine now does the same for an explicit content z-index, checks current connected nodes before writing, and preserves its existing `z-50` class when content has no explicit stacking level. | `src/overlays/base/popper.test.tsx` — explicit and default z-index tests |
| `overlay-popper` | Modal background isolation, scroll locking, and global interaction resources require a mounted content surface | ported | Kobalte Popover content uses `createHideOutside` and prevent-scroll only while present. Moraine now reuses the Modal foundation's mutation-aware `acquireAriaHideOutside` and reference-counted scroll lock, and an open root with no surface owns no global listener or stack entry. | `src/overlays/base/popper.test.tsx` — modal isolation, no-surface, forceMount, and conditional-unmount tests |
| `overlay-popper` | Outside pointer and Escape dismissal honor target cancellation, top-layer ownership, and context-menu presses | ported | Kobalte's interact-outside and escape primitives are cancelable and its layer stack ignores context-menu pointerdown. Moraine's Popper-owned handlers now run after target handlers, honor `defaultPrevented`, ignore secondary/Control-click presses, and prevent a handled Escape while retaining descendant-overlay/top-layer checks. | `src/overlays/base/popper.test.tsx` — cancellation, secondary pointer, and handled Escape tests; all menu/select stack-competition suites |
| `overlay-popper` | Virtual/context anchors and arrow positioning are not added to the Popper public contract | intentional-divergence | Kobalte exposes virtual anchor rectangles and Arrow, while Base UI accepts element, ref, virtual, function, and cursor anchors. Moraine has no Popper consumers for those APIs: ContextMenu owns cursor-reference positioning in `base/menu`, and current Popover/Tooltip/Popup/Select contracts use concrete triggers. Adding unused public surface would violate the plan. | ContextMenu virtual-reference tests and public typecheck; 210 affected consumer tests pass |
| `overlay-popper` | Native layout shift, nested scrolling, visual viewport changes, detachment occlusion, and portal hydration DOM | unverified-platform | Base UI marks geometry assertions browser-only and its auto-update path uses real `ResizeObserver`/`IntersectionObserver`; jsdom cannot prove layout, clipping, native observer delivery, or hydration keys. Moraine reuses Floating UI and the pinned guards, while production hydration inspection remains blocked by the pre-existing client-only `solid-toaster` SSG import failure. | 18 Popper tests, 210 affected consumer tests, and the full 76-file/1184-test suite pass; existing navigation warning unchanged. |
| `overlay-menu` | Root and submenu open state, active-item ownership, sibling closure, controlled roots, and radio-group synchronization | ported | Base UI `menu/root/MenuRoot.tsx` centralizes tree state and `radio-group/MenuRadioGroup.tsx` treats the group value as controlled; Kobalte `menu/menu.tsx` and `menu-radio-group.tsx` provide the Solid ownership model. Moraine retains its wrapper-level root contract, closes every sibling including a pending hover-open, snapshots uncontrolled radio defaults once, and clears a controlled group when all item `checked` values become false. | DropdownMenu controlled-root, radio-clear, pending-root-close, and pending-disabled tests; deepest-submenu and pointer-grace tests |
| `overlay-menu` | Arrow, Home, End, first/last opening focus, item removal, empty lists, and all-disabled lists | verified | Base UI `menu/root/MenuRoot.test.tsx` covers boundary navigation and Kobalte `list/list-keyboard-delegate.ts` supplies the same Solid collection traversal. Moraine delegates these keys to the frozen selectable-navigation foundation and now directly proves recovery after removal and no-op behavior for all-disabled collections. | `src/overlays/base/menu/menu.utils.test.tsx` — removal/all-disabled test; DropdownMenu keyboard-open, selection, and submenu tests |
| `overlay-menu` | Disabled items are skipped by keyboard and text navigation | intentional-divergence | Kobalte `list/list-keyboard-delegate.ts` skips disabled nodes for arrows, boundaries, and search, while fixed Base UI `menu/root/MenuRoot.test.tsx` intentionally allows disabled items to receive focus but prevents activation. Moraine preserves its existing cross-component selectable-navigation contract and Kobalte's behavior, avoiding a menu-only focus rule that would conflict with Select and other consumers. | Shared selectable-navigation disabled tests; helper typeahead disabled test; DropdownMenu and ContextMenu disabled-selection tests |
| `overlay-menu` | Locale-aware typeahead, multi-character prefixes, repeated-character cycling, timeout reset, rendered text fallback, and detached-item safety | ported | Base UI `menu/root/MenuRoot.tsx` composes Floating UI `useTypeahead` with a reset window, while Kobalte `selection/create-type-select.ts` and `list/list-keyboard-delegate.ts` normalize search through the current collection and skip disabled nodes. Moraine now owns equivalent menu-specific search without imposing text semantics on Select, resolves rendered JSX text before descriptions, and ignores disconnected registrations. | `src/overlays/base/menu/menu.utils.test.tsx` — normalization/cycling/timeout test; DropdownMenu typeahead, rendered-label, and ContextMenu typeahead tests |
| `overlay-menu` | Space activates normally but remains part of an active typeahead session for leaf, checkbox, radio, and submenu items | ported | Fixed Base UI has dedicated active-typeahead Space regressions in `MenuRoot.test.tsx`, `MenuCheckboxItem.test.tsx`, `MenuRadioItem.test.tsx`, and `MenuLinkItem.test.tsx`. Moraine now handles typeahead in the content capture phase, so item activation observes the prevented event while a plain Space still follows the existing activation path. | DropdownMenu active-search leaf/checkbox/submenu test; checkbox/radio keyboard-selection tests; Base UI evidence covers the equivalent radio/link branches |
| `overlay-menu` | RTL submenu keys and placement are logical; Escape closes the current depth and restores its owner | ported | Base UI `menu/submenu-trigger/MenuSubmenuTrigger.test.tsx` maps ArrowRight in LTR and ArrowLeft in RTL; Kobalte `menu-sub-trigger.tsx` uses locale direction for its open key. Moraine no longer infers the key from a stale physical placement and aligns submenu placement with the current document direction. Existing Escape handling remains depth-first and prevents a handled event. | DropdownMenu RTL-open, arrow open/close, deepest-submenu Escape, and trigger-focus restoration tests |
| `overlay-menu` | Tab closes the full menu tree and transfers focus forward or back in document order | ported | Fixed Base UI `menu/root/MenuRoot.test.tsx` closes and focuses the next element on Tab and the trigger on Shift+Tab. Kobalte `menu-content-base.tsx` instead traps Tab; Moraine follows Base UI because wrapper menus are transient document controls, excluding still-present portal branches while exit motion completes. | DropdownMenu forward Tab and Shift+Tab tests; Select/MultiSelect suites prove the generic listbox paths retain their separate Tab contracts |
| `overlay-menu` | Mouse hover highlighting, touch/pen exclusion, delayed submenu opening, pointer grace, and pending-timer cleanup | ported | Kobalte `menu-item-base.tsx` and `menu-sub-trigger.tsx` gate hover work to `pointerType === "mouse"`, clear delayed opens on leave/disposal, and retain a pointer grace corridor. Fixed Base UI covers the same hover cancellation and sibling behavior. Moraine now ignores touch/pen hover, cancels timers on close/disable/disposal, and closes pending sibling work through the shared submenu registry. | DropdownMenu touch/pen, pending-close, pending-disabled, and pointer-grace tests; direct safe-polygon helper tests |
| `overlay-menu` | Outside pointer/focus, Escape, nested portals, and competing overlay dismissal honor cancellation and surface ownership | ported | Kobalte's dismissable-layer callbacks are cancelable and Base UI's `useDismiss` is scoped to the active floating tree. Moraine now waits for target handlers, honors `defaultPrevented`, registers only after content exists, and reuses the frozen top-overlay/descendant-branch stack so a nested portal is inside its owning menu. | DropdownMenu outside-pointer cancellation and nested-portal tests; existing DropdownMenu, ContextMenu, Select, and MultiSelect competition suites |
| `overlay-menu` | Context-menu right click, keyboard invocation, long press, movement tolerance, and disabled/canceled gesture cleanup | verified | Fixed Base UI `context-menu` delays outside dismissal after touch/context opening, while Kobalte `context-menu-trigger.tsx` owns keyboard context-menu and touch long-press state. Moraine's ContextMenu already covers its documented 700 ms long press, movement threshold, prevented contextmenu/pointerup cleanup, repeated right click, and disabled transitions. | ContextMenu keyboard shortcut, long-press, cancellation, tolerance, repeated-trigger, and disabled tests |
| `overlay-menu` | Menu, item, checkbox, radio, group, label, separator, trigger, and submenu ARIA relationships | ported | Base UI `menu/group-label/MenuGroupLabel.test.tsx` and Kobalte `menu-group.tsx` link groups with `aria-labelledby`; both references expose stable item/content ids plus the expected roles and checked/expanded states. Moraine now assigns ids to every interactive item, labels groups, and labels root/submenu content from the actual trigger id while retaining its existing roles and state attributes. | DropdownMenu group-label/item-id test plus item-matrix and trigger/submenu semantic tests; ContextMenu explicit/generated id tests |
| `overlay-menu` | Shared positioning and dismissal utilities remain semantic-neutral for Select | ported | Kobalte Popper uses an absolute strategy and both references keep menu typeahead/activation outside generic positioning. Moraine's shared floating helper now uses one absolute strategy with stale-result and observer cleanup guards, while Select supplies its own content/trigger surfaces only for stack-safe dismissal and retains listbox keyboard behavior. | DropdownMenu absolute positioner test; Select z-index/positioning tests; 97 Select/MultiSelect dependency tests |
| `overlay-menu` | JSX labels and render values preserve single evaluation and conditional construction order | ported | Kobalte's Solid menu content is guarded by presence and obtains text from mounted label/content refs; the Moraine SSR gate requires raw JSX/getter values to be cached before conditional reads. Group labels are now memoized once, and typeahead reads the mounted DOM for non-string labels without re-invoking render props. | DropdownMenu group-label getter and JSX-label typeahead tests; existing itemRender and closed-presence tests |
| `overlay-menu` | Base UI event-detail/render APIs, controlled submenu roots, and menu-specific disabled-focus behavior are not added to wrapper contracts | intentional-divergence | The plan preserves Moraine's item-array wrappers and excludes Base UI's low-level render/event-detail surface. Adding cancelable change-detail objects, detached triggers, or a second submenu-control API would replace documented Moraine contracts rather than close an observable compatible gap; cancellation continues through existing DOM/item props and wrapper callbacks. | Public typecheck/type tests; content/item cancellation tests; controlled root tests |
| `overlay-menu` | Native delayed touch clicks, actual pointer capture/drag release, geometry-driven safe polygons, visual viewport positioning, screen-reader announcement order, and portal hydration DOM | unverified-platform | The fixed upstream suites mark text/layout and device-specific paths as browser-only, while jsdom cannot reproduce iOS delayed clicks, native pointer capture, real layout corridors, assistive technology, or hydration keys. Deterministic ownership, timing, cancellation, ARIA, and zero-geometry behavior are covered locally; production hydration inspection remains blocked by the pre-existing client-only `solid-toaster` SSG import failure. | 5 helper tests, 75 DropdownMenu/ContextMenu tests, 97 Select/MultiSelect dependency tests, and the full 76-file/1200-test suite pass; existing navigation warning unchanged. |
| `accordion` | Single/multiple controlled and uncontrolled disclosure state, non-collapsible last item, absent controlled values, and callback ordering | verified | Base UI `accordion/root/AccordionRoot.tsx` computes the next value before publishing it, while Kobalte `accordion-root.tsx` delegates single/multiple selection to one controlled list state. Moraine's frozen controllable/disclosure foundations already preserve controlled UI, snapshot defaults, avoid callbacks for a non-collapsible open item, and publish one next-value array per accepted activation. | Existing Accordion single/multiple, controlled, non-collapsible, disabled, and callback tests; controllable/disclosure dependency suites |
| `accordion` | Missing item values remain stable across reorder and duplicate explicit values retain unique trigger/panel ids | ported | Base UI `accordion/item/AccordionItem.tsx` generates an owner-stable fallback value and generates part ids independently from the item value; Kobalte requires an explicit value but separately registers trigger/content ids. Moraine previously used the current array index for both state and ids, which moved open state during reorder and duplicated DOM ids. Each item owner now receives a stable fallback value and collision-free id segment while duplicate explicit values continue to share their documented value state. | Accordion generated-value reorder/focus test and duplicate-value ARIA-id test |
| `accordion` | Enter activates once on keydown; Space activates once on keyup; key repeats and orphan keyups do not duplicate changes | ported | Fixed Base UI `accordion/root/AccordionRoot.test.tsx` explicitly proves Space keyup timing for native and non-native triggers, and Kobalte `accordion-trigger.tsx` prevents native button synthesis before delegating selection. Moraine now tracks one Space press, prevents the synthetic click path, ignores repeated Enter, and resets pending Space on blur or pointer click. | Accordion Space timing/repeat test plus existing Enter, click, disabled, and controlled callback tests |
| `accordion` | ArrowUp/ArrowDown/Home/End navigation, disabled skipping, natural Tab order, loop boundaries, and nested scoping | intentional-divergence | Fixed Base UI deprecated and removed accordion roving-arrow behavior after the APG update, while Kobalte `accordion-root.tsx` still composes `createSelectableList` with wrapping and Tab navigation. Moraine preserves its documented `loopFocus` API and existing Kobalte-compatible keys, but now derives navigation from direct owned trigger DOM so nested accordions and duplicate values cannot capture focus. | Accordion navigation, no-wrap, disabled, natural button semantics, and nested-scoping tests |
| `accordion` | Focus survives keyed reorder and moves to the nearest enabled owned trigger after reactive removal or disablement | ported | Kobalte's DOM collection and selection manager retain item ownership across collection updates and clear invalid focused keys; Base UI's current natural-focus model no longer provides roving recovery. Moraine now keys item owners by the source item, preserves the same trigger node during reorder, and performs a guarded post-update recovery only when focus fell to the body or remained on the newly invalid trigger, so unrelated outside focus is never stolen. | Accordion generated reorder/focus and reactive disable/removal recovery tests |
| `accordion` | Native heading, button, region, expanded/controls/labelledby relationships, state attributes, and disabled propagation | ported | Base UI `AccordionHeader.tsx` and Kobalte `accordion-header.tsx` default to `h3`; both references register unique trigger/panel ids and apply region labelling plus open/disabled state. Moraine previously used `div role="heading"` without an ARIA level and value-derived duplicate ids. It now renders `h3`, preserves open-only `aria-controls`, and maintains unique bidirectional relationships through exit presence. | Accordion native-heading, stable ARIA, duplicate-id, exit-controls, and disabled-state tests |
| `accordion` | Root/item disabled triggers are non-interactive and skipped by navigation | verified | Kobalte's collection marks disabled triggers and skips them in its keyboard delegate. Base UI keeps disabled triggers focusable but blocks activation; Moraine intentionally retains its documented native `disabled` button semantics, which are already shared by its item-array controls, while all pointer, synthetic click, Enter, and Space paths check the same reactive disabled state. | Accordion disabled navigation, root/item disabled callback, reactive-disable focus, and state-attribute tests |
| `accordion` | Open measurement, equal-height switching, interrupted close/reopen, exit retention, reduced motion, zero-height content, and content removal | verified | Base UI `accordion/panel/AccordionPanel.test.tsx` retains a closing panel through its transition and suppresses stale open animation; Kobalte composes its audited Collapsible presence/measurement primitives. Moraine already delegates to the frozen disclosure and transition foundations, measures each mounted panel owner, retains closing content until completion, and cancels stale completion on reopen or disposal. | Accordion controlled measurement, equal-height switch, and exit-retention tests; disclosure and transition-presence dependency suites |
| `accordion` | Item label/content JSX is resolved once per mounted value and closed unmounted content is not instantiated | ported | Kobalte's Solid content remains inside its Collapsible presence branch. The Moraine SSR gate requires JSX getters to be cached before conditional inspection without eagerly creating closed content. Accordion now memoizes visible item labels and creates the content memo only inside the mounted panel branch, while the item collection itself is read through one memo. | Accordion getter-backed label/content test; closed/open and `unmountOnHide` tests |
| `accordion` | Empty collections, all-disabled collections, empty content, duplicate values, and content removed with its item remain deterministic | verified | Both references treat collection emptiness and missing panels as valid composition states; Kobalte's keyboard delegate returns no target when none is enabled. Moraine's owned-trigger scan is a no-op without an enabled target, optional content omits its inner wrapper, duplicate values have unique part ids, and keyed item disposal releases measurement/presence/id ownership synchronously. | Accordion all-disabled navigation, duplicate-id, optional-content, reactive removal, and unmount tests |
| `accordion` | Base UI compound-part event details, per-item event cancellation, manual part ids, `hiddenUntilFound`, and polymorphism are not added | intentional-divergence | The plan preserves Moraine's data-driven item-array API. Base UI's cancelable `onOpenChange` details and manual compound parts, plus Kobalte's polymorphic parts, cannot be exposed without replacing that public contract; existing root DOM props cannot cancel an earlier target-phase item handler, so cancellation remains unavailable rather than adding a partial event API. Moraine also retains documented `collapsible=true` and `unmountOnHide` defaults. | Public typecheck/type tests and documented Accordion API; no new props or part exports |
| `accordion` | Real native touch/click synthesis, browser transition geometry, generated hydration keys, and assistive-technology announcement order | unverified-platform | Fixed Base UI skips multiple interaction, layout, and hydration assertions in jsdom, and the SSR gate states that jsdom cannot validate hydration creation keys. Local tests prove event deduplication, deterministic Solid ownership, single getter evaluation, zero-geometry measurement, and ARIA structure; production SSG inspection remains blocked before the Accordion route by the pre-existing client-only `solid-toaster` server import failure. | 29 Accordion tests, 30 shared dependency tests, typecheck, and `git diff --check` pass; production blocker unchanged. |
| `avatar` | Missing/blank source, async loading, success, error, and cached `complete`/`naturalWidth` outcomes | ported | Fixed Base UI `avatar/image/useImageLoadingStatus.ts` probes with `window.Image` and immediately resolves a complete cached image from `naturalWidth`; Kobalte `image-img.tsx` supplies the Solid owner/cancellation pattern. Moraine now implements the cached fast path, treats normalized blank sources as errors without allocating a loader, and never publishes `idle` through `onStatusChange`. | Avatar loading/success/error, cached-success/error, blank-source, and status-callback tests |
| `avatar` | Normalized source changes, rapid replacement, unmount cleanup, and reactive callback ownership | ported | Both references scope each probe to the current source owner and ignore callbacks after cleanup. Moraine now memoizes the trimmed source so whitespace-only changes do not restart loading, reads `onStatusChange` untracked so callback replacement does not recreate the probe, delivers completion to the latest callback, and rejects stale load/error events after replacement or disposal. | Avatar whitespace-equivalent source, callback replacement, source reset, and stale replacement/disposal tests |
| `avatar` | Loading-status callbacks are ordered and deduplicated | verified | Base UI reports non-idle probe states from a layout effect and Kobalte publishes each non-idle signal state through its root context. Moraine's state setter already suppresses identical statuses; the source memo and untracked callback fix remove the only spurious restart paths, while cached outcomes resolve without an idle transition. | Avatar async success/error exact callback sequences, cached outcome, blank source, callback replacement, and stale-event tests |
| `avatar` | Exactly one image semantic is exposed during loading/error/loaded states while the existing crossfade layers remain mounted | ported | Base UI and Kobalte mount fallback semantics until the image loads and then expose the image. Moraine intentionally retains both visual layers for its documented opacity transition, but now marks the inactive layer `aria-hidden`, gives a meaningful fallback `role="img"`/label, and removes the role for decorative empty-alt fallback. This closes duplicate announcements without changing visual transition design. | Avatar accessible fallback-to-image, loaded fallback-hidden, loading image-hidden, and decorative empty-alt tests |
| `avatar` | Caller `aria-label` and `aria-hidden` semantics override descendant image/fallback naming | ported | The references allow semantics to be placed on their compound root. Moraine's data-driven root now becomes the single image semantic when a caller supplies `aria-label`, and both internal layers become hidden from the accessibility tree; caller role and `aria-hidden` attributes remain forwarded and authoritative. | Avatar caller-root-label test plus arbitrary-root-props coverage |
| `avatar` | Meaningful/decorative `alt`, explicit text, icon fallback, Unicode whitespace, and unusual initials | ported | Base UI forwards native image alt and leaves fallback content to the caller; Kobalte does the same. Moraine preserves its documented automatic initials extension, but now tokenizes all Unicode whitespace and slices code points rather than UTF-16 units, so emoji and surrogate pairs are never split. Cached raw alt/text/fallback values are reused for visible and accessible output. | Avatar alt initials, Unicode initials, fallback icon, decorative alt, and getter single-evaluation tests |
| `avatar` | Avatar and badge remain passive, non-tabbable presentation; badge icons do not intercept pointer events | ported | Neither upstream primitive adds keyboard or pointer interaction. Moraine already creates no internal tab stop or handlers; the absolutely positioned badge could still become the pointer target, so it now uses `pointer-events-none` while its Icon remains `aria-hidden`. | Avatar passive badge/no-tab-stop test and Icon dependency suite |
| `avatar` | Root/image/fallback/badge status slots and visual transition classes remain Moraine-owned | intentional-divergence | Base UI unmounts inactive parts and exposes a fallback-delay API, while Kobalte conditionally mounts parts. Moraine's public `transition` variants require the existing overlapping image/fallback DOM for crossfade and the plan forbids visual redesign, so DOM mount exclusivity and upstream delay props are not ported; accessibility exclusivity and loading behavior are equivalent. | Existing transition/class/style tests plus accessible exclusivity regressions |
| `avatar` | Native responsive image props and compound-part APIs are not added | intentional-divergence | Fixed Base UI accepts `srcSet`, `sizes`, `crossOrigin`, and `referrerPolicy` on `Avatar.Image`, while Kobalte exposes polymorphic Root/Img/Fallback parts. Moraine's documented item API contains only `src`, `alt`, text/icon fallback, badge, and status callback; adding native probe props or compound parts would expand the preserved contract. | Public typecheck/type contract test; `AvatarFace` compatibility surface remains exported |
| `avatar` | AvatarGroup empty/max/reverse/count behavior and per-face loading ownership remain Moraine-specific | verified | Neither reference has a direct group counterpart. Moraine caches the group item collection once, derives visible and hidden slices without mutation, and gives every visible `AvatarFace` its own source owner; empty groups still render no root and size/count/item slots remain unchanged. | Existing AvatarGroup empty, max, reverse, sizes, slots, and style tests |
| `avatar` | Raw source/alt/text/fallback/badge values are single-evaluated and server markup stays deterministic | ported | Kobalte reads its Solid props through accessors owned by the image/fallback components, and the Moraine SSR gate requires repeated conditional inputs to be memoized. Avatar now caches every shared fallback input and AvatarGroup caches its item collection, while browser probing remains inside `createEffect`, so server construction has no `window.Image` access and produces stable idle/fallback layers. | Avatar getter-backed single-evaluation, blank-source guard, and group collection tests |
| `avatar` | Real decoded-cache hydration without fallback flash, browser image event ordering, crossfade pixels, and screen-reader announcements | unverified-platform | Fixed Base UI keeps the cached hydration case browser-only; jsdom can model `complete`, `naturalWidth`, cancellation, DOM/ARIA ownership, and callbacks but cannot prove decoded cache timing, paint suppression, actual image fetch events, opacity pixels, or assistive technology. Production inspection remains blocked by the pre-existing client-only `solid-toaster` SSG import failure. | 29 Avatar/AvatarGroup tests, 11 Icon dependency tests, typecheck, and `git diff --check` pass; production blocker unchanged. |
| `badge` | Neutral text, arbitrary root attributes, caller `aria-label`/`aria-hidden`, and no implicit tab stop | verified | Fixed Kobalte Badge gives its specialized badge root status semantics; Moraine's comprehensive metadata Badge forwards native span attributes and remains non-interactive unless the optional trailing action exists. Existing caller semantics remain authoritative. | Badge default semantics, decorative hiding, root override, and no-action structure tests |
| `badge` | Mandatory Kobalte `role="status"` and `textValue` API are not adopted | intentional-divergence | Fixed Kobalte `packages/core/src/badge/badge-root.tsx` always renders `role="status"` and derives `aria-label` from `textValue`. Moraine Badge is also used for static counts, variants, branches, and metadata where live-status semantics would be incorrect; its documented API explicitly excludes `textValue`. | Kobalte fixed-source and accessible-name tests; Moraine root-semantic tests |
| `badge` | Trailing action is a native `type="button"` with a stable accessible name and decorative icon | ported | Fixed Base UI Button tests establish native-button focus and Enter/Space activation behavior. Moraine already uses `IconButtonInner`; Badge now labels the action from `title`, primitive label content, or `Remove badge`, while the glyph stays `aria-hidden`. | Badge trailing activation, title/label fallback, native element, and Icon accessibility tests |
| `badge` | Standalone root pointer events preserve native default and caller/ancestor propagation | ported | Neither upstream Badge nor native Base UI Button cancels pointerdown on a passive surrounding span. Moraine removed its unconditional root cancellation, so callers can prevent or stop the event themselves and unrelated standalone composition is no longer blocked. | Badge standalone pointer default, caller ordering, and ancestor propagation test |
| `badge` | MultiSelect tag label pointerdown reaches the control, while remove pointerdown preserves input focus | ported | The fixed button evidence relies on native pointer behavior; tag-specific focus ownership belongs to the consumer. MultiSelect now lets label presses reach its control handler, but intercepts only presses originating in the remove action, prevents button focus transfer, stops control toggling, and explicitly restores input focus. | MultiSelect tag-label focus and remove-focus tests |
| `badge` | Disabled MultiSelect tags expose no active remove control or callback path | ported | Fixed Base UI Button suppresses disabled activation and removes native disabled buttons from tab order. Badge has no disabled contract, so MultiSelect now omits `onTrailingClick` while disabled; the same trailing glyph remains decorative and no button or removal callback exists. | MultiSelect disabled tag-removal test |
| `badge` | One click/touch-derived click produces one trailing callback without toggling the enclosing select | verified | The trailing handler is attached only to the native button and stops click propagation before changing selection. Root pointer handling no longer duplicates activation, and reactive action replacement uses one cached handler value. | Badge exact click count, reactive action, and MultiSelect exact removal count tests |
| `badge` | Zero, empty/falsy children, icon-only output, and missing icon/action combinations | ported | Fixed Kobalte renders children directly. Moraine keeps optional label wrappers, but now treats numeric zero as content while null, undefined, false, and empty string omit the wrapper; a trailing icon without a handler remains decorative, and a handler without `trailing` intentionally creates no invisible action. | Badge zero, default/empty structure, icon-slot, and reactive action tests |
| `badge` | JSX children, leading/trailing icons, title, and action handler are single-evaluated | ported | The Moraine SSR gate requires inspected JSX and conditional values to be cached before branch selection. Badge now resolves children once and memoizes every optional input reused across rendering or accessible naming, including reactive action replacement. | Getter-backed exact-read test and reactive action test |
| `badge` | Server/client slot creation order and reactive action hydration | ported | The production SSR compiler renders hydratable `leading`, `label`, and decorative `trailing` nodes in the same order consumed by the client. The reusable isolated SSR fixture proves the root is reused rather than replaced, then verifies post-hydration action add/click/remove behavior; Vitest now emits hydratable client code for these gates. | Badge isolated `renderToString -> hydrate -> action` regression |
| `badge` | Public `TrailingButtonProps`, fixed variants, slots, and comprehensive display API remain Moraine-owned | intentional-divergence | Kobalte has no removable action and Base UI exposes a full standalone Button API. Moraine preserves its existing Badge variants, slot overrides, `onTrailingClick`, and declared `TrailingButtonProps` surface without exposing polymorphism, `textValue`, or nested button configuration in this pass. | Badge public export, variants, classes/styles, and type suites |
| `badge` | Real-device touch-to-click synthesis, focus visuals, and assistive-technology announcements | unverified-platform | jsdom proves cancelability, propagation, focus ownership, DOM/ARIA output, hydration, and exact callbacks, but it does not generate mobile touch/pointer compatibility clicks, paint focus indicators, or run a screen reader. Those claims require production browser/device inspection, which remains blocked by the pre-existing client-only `solid-toaster` SSG import failure. | 15 Badge tests, 37 MultiSelect consumer tests, 11 Icon tests, typecheck, and `git diff --check` pass; production blocker unchanged. |
| `button` | Native button/input type, disabled attribute, busy state, form-safe default, and no redundant role | verified | Fixed Base UI defaults native buttons to `type="button"` and native `disabled`; fixed Kobalte additionally preserves native input type/disabled handling. Moraine already matches those branches and exposes loading through `aria-busy` plus the existing disabled mechanism. | Button default, input/type contract, disabled/loading, form-safe type, and loading-state tests |
| `button` | Non-native div, anchor-without-href, and custom-component role/tab/ARIA semantics | ported | Fixed Base UI applies button semantics when configured non-native, and fixed Kobalte applies them whenever the rendered root is neither a native button nor a valid link. Moraine now treats custom components without `href` as non-native buttons, preserves caller role/tab order while enabled, removes the tab stop when disabled/loading, and retains `aria-disabled`. | Button div, anchor-without-href, custom component, caller role/tab, reactive root, and disabled/loading tests |
| `button` | Enter keydown, Space keydown/keyup timing, page-scroll prevention, and one real click | ported | Fixed Base UI dispatches Enter on keydown, prevents Space scrolling on keydown, and activates non-composite Space on keyup. Moraine now arms Space per component instance, requires its eligible keydown, activates once on keyup, and keeps Enter on keydown. | Button Enter, Space timing, missing-keydown, repeat-safe single activation, and exact click-count tests |
| `button` | Keyboard modifier propagation, nested-event scoping, and consumer cancellation/order | ported | Fixed Base UI `dispatchClickWithModifiers` carries modifier state and only activates when `target === currentTarget`; user key handlers run first and may cancel internal behavior. Moraine now dispatches a composed cancelable MouseEvent with all modifiers, ignores bubbled child keys, and honors both keydown and keyup prevention before activation. | Button modifier, nested child, keydown cancel, keyup cancel, and keyup-before-click ordering tests |
| `button` | Native links and router links retain link semantics; Space does not synthesize activation | intentional-divergence | Fixed Base UI's `nativeButton={false}` always exposes button semantics and adds Space activation even to a rendered link. Fixed Kobalte instead omits button role/tab handling for `<a>` with any `href`; Moraine preserves that established link contract for native and Solid Router links, including empty `href`, so Enter/navigation remain browser-owned. | Button native anchor, empty href, Solid Router `A`, tuple click, and Space non-activation tests |
| `button` | Disabled/loading activation suppresses click, pointerdown, and keyboard user handlers | ported | Fixed Base UI returns before external activation handlers when disabled for native and custom roots. Moraine now checks disabled/loading before user key and pointer handlers, prevents non-native activation defaults without stopping unrelated ancestor propagation, and blocks programmatic click delivery for every root kind. | Button native/non-native disabled key, pointer, click, loading, and post-hydration activation tests |
| `button` | No `focusableWhenDisabled` API; disabled non-native roots leave the tab sequence | intentional-divergence | Base UI exposes `focusableWhenDisabled`, while fixed Kobalte has no equivalent and removes `tabindex=0` for disabled non-native buttons. Moraine's preserved public contract has no focusable-disabled option and continues the Kobalte behavior; adding the upstream prop would be an API migration outside this plan. | Button disabled/loading tab-index tests and public type suites |
| `button` | Pointer up/cancel/leave and context-menu events stay native and caller-owned | verified | Fixed Base UI only intercepts activation-related click, key, mouse-down, and pointer-down paths. Moraine forwards completion, cancellation, leave, and context-menu handlers unchanged and keeps no pointer gesture state that could duplicate click/touch activation. | Button pointer completion/cancel/leave/context-menu forwarding test |
| `button` | Controlled loading, async auto-loading, overlap accounting, rejection/throw, stale disposal, and rapid repeat blocking | verified | Loading is Moraine-owned rather than an upstream Button API. The completed `use-loading-auto` foundation counts all pending thenables, settles resolve/reject symmetrically, ignores stale owner completion, and does not engage for sync/throw/prevented handlers; Button disables repeat activation as soon as the first async action is pending. | 7 loading-helper tests plus Button async lifecycle, reactive render child, sync, repeat, and controlled-loading tests |
| `button` | Group defaults/overrides, orientation, separators, and overlay-trigger composition | verified | Neither upstream Button primitive owns Moraine's comprehensive ButtonGroup. Group context remains reactive, child overrides win, separators stay decorative, and overlay trigger roots remain direct group children. | 27 ButtonGroup tests and overlay trigger regressions |
| `button` | Children/render props and leading/trailing/loading icons are single-evaluated and remain reactive | ported | The Moraine SSR gate requires two-stage resolution for `ComponentOrElement` children and memoized arbitrary JSX slots. Button retains that owner-safe resolution, caches every icon input, keeps render components mounted once across loading changes, and now renders numeric zero while still omitting false/empty output. | Button getter exact-read, render-child lifecycle, reactive JSX, zero/falsy, icon slot, and icon-only naming tests |
| `button` | SSR tag/slot order, render-child output, and hydration activation remain deterministic | ported | An isolated production SSR fixture now generates hydratable Button markup, and the client gate proves the root is reused, the render child changes with loading, disabled activation is suppressed, and click resumes afterward. The shared Vitest compiler emits hydratable client code so this gate checks actual keys rather than jsdom-only rendering. | Button `renderToString -> hydrate -> loading/click` regression and reusable SSR fixture runner |
| `button` | Unsupported input types and polymorphic API shape remain Moraine-owned | intentional-divergence | Fixed Kobalte distinguishes button-capable input types, while Base UI requires callers to declare native versus non-native behavior. Moraine's preserved generic `as` contract already accepts native input types and treats input roots as browser-native controls; this plan does not add Base UI `render`/`nativeButton`, remove accepted types, or perform post-ref DOM inference. | Public default/autocomplete type suites and native input behavior audit |
| `button` | Variants, loading visuals, icon placement, press motion, and slot classes remain unchanged | intentional-divergence | Upstream primitives do not define Moraine's styling or loading-icon policy, and the plan excludes visual changes. Existing variants, group inheritance, icon replacement rules, and transition classes therefore remain Moraine-specific. | Button variant, size, class/style, loading-icon placement, and ButtonGroup visual contract tests |
| `button` | Browser-generated native keyboard clicks, form submission/navigation, focus after disabling, touch synthesis, and visual focus state | unverified-platform | jsdom can prove constructed non-native events, DOM attributes, handler order, hydration, and state transitions but does not faithfully generate native button/link keyboard defaults, navigate documents, synthesize mobile compatibility clicks, submit across engines, or paint focus-visible state. | 67 Button tests, 27 ButtonGroup tests, 7 loading-helper tests, representative consumer suites, typecheck, and `git diff --check` pass; only the existing jsdom navigation warning remains. |
| `collapsible` | Static trigger content gets a native button; render triggers receive one reactive flattened contract | ported | Fixed Base UI and Kobalte provide a native trigger part with button semantics. Moraine preserves its comprehensive `triggerRender` API, now caches the raw trigger value before type inspection/rendering, wraps static JSX in one native button, and mounts component renderers once with reactive `isOpen`, disabled, controls, and `triggerProps`. | Collapsible static/function/reactive trigger, exact getter-read, flattened controls, and native structure tests |
| `collapsible` | Native Enter/Space activation and one pointer/touch-derived click toggle | verified | Both references delegate trigger activation to their audited Button primitive. Moraine's built-in and documented custom path expose a native `type="button"`; the sole `triggerProps.onClick` toggles once and checks `defaultPrevented`, so browser-generated keyboard/click activation cannot double-deliver internally. | Collapsible static/native trigger, plain trigger, exact callback-order, and canceled-click tests |
| `collapsible` | Custom triggers that omit `triggerProps` or use invalid nested interactive content are consumer-owned | intentional-divergence | Base UI compound Trigger owns the rendered interactive root, while Moraine deliberately exposes a render contract and documents that `triggerProps` must be spread on the interactive element. It does not infer a root, inject keyboard semantics into arbitrary fragments, or suppress bubbling from invalid nested controls. | Public `TriggerRenderProps` contract and render-trigger scoping tests |
| `collapsible` | Disabled state suppresses native trigger, render controls, toggles, and callbacks reactively | verified | Fixed Base UI/Kobalte prevent disabled trigger changes. Moraine's shared disclosure state drives native `disabled`, `data-disabled`, and every `open`/`close`/`toggle` request through the same guard, including state changes after mount. | Collapsible initial/reactive disabled, trigger click, and render-control tests |
| `collapsible` | Controlled requests do not self-mutate; uncontrolled changes publish once in order | verified | Both fixed references keep controlled state externally owned and call `onOpenChange` for each eligible request. The completed controllable-value foundation preserves that split; Collapsible deduplicates requests equal to the current state and emits exact true/false order for uncontrolled toggles. | Collapsible controlled request, uncontrolled callback sequence, default-open, and controllable-value dependency tests |
| `collapsible` | Cancelable Base UI change-event details are not added; native event cancellation remains supported | intentional-divergence | Fixed Base UI passes `ChangeEventDetails.cancel()`, while Kobalte and Moraine expose `(open: boolean) => void`. The preserved Moraine contract cannot add event details in this pass; it honors an already canceled native click before requesting state, but callback cancellation is intentionally unavailable. | Fixed Base UI cancellation tests/source and Collapsible capture-canceled click regression |
| `collapsible` | Deterministic root/trigger/content IDs and open-only ARIA relationships | verified | Fixed Base UI registers the current panel ID and only publishes `aria-controls` while open; Kobalte generates root-derived content IDs and does the same. Moraine derives stable trigger/content IDs from one `useId`, exposes `aria-expanded`, restores `aria-controls` on remount, and labels content from its trigger. | Collapsible explicit/generated ID, open/closed controls, hydration relationship, and nested-uniqueness tests |
| `collapsible` | Nested state, controls, IDs, and focus ownership remain independent | verified | Both upstream implementations scope disclosure through a root context. Moraine creates all state and IDs per component owner; activating an inner trigger leaves the outer disclosure unchanged, and content mount/unmount never programmatically moves trigger focus. | Collapsible nested-state/ID test and trigger-only toggle tests |
| `collapsible` | Exit retention, transition cancel/end, no-motion, interruption, stale element, and replacement behavior use the shared foundation | ported | Fixed Base UI has extensive interrupted motion coverage and Kobalte uses `createPresence`. Moraine routes this behavior through the completed transition-presence foundation, now including event-name identity filtering; a controlled close/reopen rejects stale completion while current content remains mounted. | Collapsible transition retention/reopen test plus 20 transition-presence tests |
| `collapsible` | Height measurement, resize changes, detached elements, and missing layout APIs use the disclosure foundation | verified | Kobalte measures content dimensions and Base UI restores measured size around motion. Moraine's intentionally height-only design delegates current-element measurement, ResizeObserver updates, queued remeasurement, replacement, cleanup, and no-layout construction to `useDisclosureState`. | 6 disclosure-state tests plus Collapsible height variable/transition integration tests |
| `collapsible` | Closed content is lazy and each real remount creates a fresh content subtree | ported | Both references conditionally mount content unless an explicit persistence API is selected. Moraine keeps the child getter inside the presence branch, so initially closed content is not instantiated, immediate close disposes it, and reopening creates a new subtree rather than caching removed DOM. | Collapsible lazy getter exact-per-mount and SSR closed/open/close/reopen tests |
| `collapsible` | SSR closed tree, hydration IDs/order, and post-hydration lifecycle are deterministic | ported | The production SSR fixture emits only the root and trigger for an initially closed disclosure. The client gate reuses that root, instantiates no content during hydration, verifies bidirectional ARIA IDs after opening, then closes and remounts content cleanly. | Collapsible isolated `renderToString -> hydrate -> open/close/reopen` regression |
| `collapsible` | Base UI `keepMounted`, `hiddenUntilFound`, `beforematch`, panel registration, compound parts, and width variables are not added | intentional-divergence | These fixed Base UI/Kobalte features require APIs explicitly excluded by the plan. Moraine retains required `triggerRender`, immediate unmount when transition is false, optional height-exit retention, fixed generated panel ownership, and only `--mo-collapsible-content-height`. | Public API/type tests and fixed upstream source audit |
| `collapsible` | Base UI's general transition/keyframe engine is not transplanted | intentional-divergence | Base UI measures width/height and coordinates arbitrary CSS transitions/keyframes, while Moraine's public `transition` flag specifically owns its existing height transition class. The plan forbids changing visual transition design, so shared interruption/safety behavior is ported without copying Base UI's animation attributes or dimension engine. | Existing transition class, slot/style, and shared presence/measurement suites |
| `collapsible` | Real layout dimensions, zero-size transition generation, initial-paint suppression, reduced motion, native keyboard/touch defaults, and browser find-in-page | unverified-platform | jsdom can mock size, observers, events, timers, IDs, focus, and hydration keys but cannot prove real scroll geometry, whether an engine generates a zero-distance transition, first-paint animation suppression, media-reduced motion CSS, native touch/keyboard click synthesis, or `beforematch`. | 27 Collapsible tests and 36 controllable/disclosure/presence/render-prop dependency tests pass; production browser inspection remains blocked by the existing `solid-toaster` SSG import failure. |
| `progress` | Native progressbar role and determinate/indeterminate ARIA ranges | ported | Fixed Base UI and Kobalte retain `aria-valuemin`/`aria-valuemax` while omitting `aria-valuenow` and value text for indeterminate progress. Moraine now applies the same split and keeps its fixed zero minimum. | Progress default ARIA, custom label, and indeterminate regression tests |
| `progress` | Null, undefined, NaN, and positive/negative infinity enter indeterminate state | ported | Both fixed references model invalid or missing current values separately from a determinate zero. Moraine now classifies every non-finite number as indeterminate instead of leaking invalid ARIA values or transforms. | Progress indeterminate boundary and synchronized-state tests |
| `progress` | Numeric and step maximum normalization, including invalid and zero ranges | ported | Fixed upstream roots sanitize invalid maxima and clamp determinate values. Moraine now falls back to 100 for invalid numeric maxima, preserves an explicit zero maximum, derives step maxima without negative ranges, and clamps below/above range consistently. | Progress numeric clamp, zero maximum, and empty/single-step tests |
| `progress` | Fractional value percentages remain exact across text, styles, and transforms | ported | Fixed Base UI/Kobalte compute a bounded ratio without integer rounding. Moraine now uses the exact bounded percentage for default value text, status sizing, and indicator translation. | Progress fractional synchronization test |
| `progress` | Loading, complete, and indeterminate data state is synchronized across rendered parts | ported | Fixed upstream parts expose root state consistently to consumers. Moraine now publishes `data-progress="loading|complete"` or `data-indeterminate` on root, status, track, indicator, steps, and step nodes within one reactive cycle. | Progress synchronized loading/complete/indeterminate state test |
| `progress` | Status renderer, value-label callback, and error-boundary behavior stay reactive | verified | Moraine-owned callbacks receive normalized live values, update when callback props change, and throw inside their owning render computation so a caller ErrorBoundary can contain failures. | Progress renderer freshness, exact-read, and thrown-label ErrorBoundary tests |
| `progress` | Step arrays handle empty, single, fractional, and duplicate labels with stable nodes | verified | Moraine's array-step API has no direct upstream equivalent. Empty and single-step ranges avoid invented negative maxima, fractional values select by the documented rounded index, and duplicate labels retain stable indexed DOM ownership through updates. | Progress empty/single-step, step renderer, and duplicate-node stability tests |
| `progress` | Rounded active-step selection and `first`/`last` renderer states remain Moraine-owned | intentional-divergence | Base UI and Kobalte expose numeric progress rather than Moraine's label-array step renderer. This sweep preserves the established rounded active index and mutually exclusive `first`, `last`, `active`, and `other` states instead of importing a new compound API. | Progress step-state and public type tests |
| `progress` | Default indeterminate value, fixed minimum zero, and absent locale/format APIs remain Moraine-owned | intentional-divergence | Fixed Kobalte defaults to determinate zero and supports configurable min/value formatting; fixed Base UI requires its own root contract. Moraine preserves `value=null`, `min=0`, `max: number|string[]`, and `getValueLabel` without adding upstream locale, format, min, or compound-part props. | Progress default/ARIA tests and public type surface audit |
| `progress` | Conditional status and step JSX is lazy and single-evaluated per mounted branch | ported | The Moraine SSR gate requires optional JSX inputs to be cached only inside the branch that consumes them. Progress now avoids reading inactive renderers, reads active renderer/value inputs once per reactive evaluation, and remounts branch-owned output only when the branch truly returns. | Progress getter exact-read and conditional renderer laziness tests |
| `progress` | SSR slot order and post-hydration determinate/indeterminate/complete updates are deterministic | ported | The isolated production SSR fixture emits status, track, and steps in the same order consumed by the hydratable client build. Hydration reuses the root and updates state and ARIA without replacing branch-stable DOM. | Progress isolated `renderToString -> hydrate -> state cycle` regression |
| `progress` | Progress remains passive and orientation is presentation-only | verified | Neither fixed upstream primitive introduces a tab stop or pointer gesture for read-only progress. Moraine forwards caller pointer events without cancellation, remains non-tabbable, and exposes orientation only through its existing data/class contract. | Progress passive pointer/tab-order and orientation tests |
| `progress` | Existing variants, animation classes, status layout, and step styling remain unchanged | intentional-divergence | Upstream primitives do not define Moraine's visual transition or step presentation. The parity sweep intentionally preserves existing size, orientation, animation, class, and style slots. | Progress animation, class, style, and thickness tests |
| `progress` | Painted animation, fractional geometry, reduced-motion behavior, and assistive-technology announcements | unverified-platform | jsdom proves computed attributes, inline styles, event passivity, reactive ordering, and hydration, but it cannot paint transitions, measure real layout, evaluate reduced-motion CSS, or run a screen reader. | Focused Progress suite, typecheck, and diff validation pass; browser/device claims remain outside the jsdom-only scope. |

### Acceptance

This matrix is accepted as the **canonical scope** for the edge-case parity
sweep. Any addition or removal must be reflected here before related parity
tasks are picked up.
