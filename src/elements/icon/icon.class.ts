import { slotRecipe } from '../../shared/style/recipe.ts'

import type { IconT } from './icon.types.ts'

export const iconRecipe = /* @__PURE__ */ slotRecipe<IconT.Slot, IconT.Variant>({
  base: {
    root: '',
  },
})
