---
title: Modal
description: Low-level modal primitives with focus management, scroll locking, dismissal control, and overridable default surface styles.
sidebar:
  order: 1
search:
  tags: [modal, primitive, overlay, focus trap, dialog primitive]
---

import { Button, Modal } from '@src'

## Import

```tsx
import { Modal } from 'moraine'
import type { ModalT } from 'moraine'
```

## Features

- **Composable Primitives**: `Modal`, `Modal.Trigger`, and `Modal.Content` separate state, trigger, and portaled content responsibilities.
- **Overridable Defaults**: `Modal.Content` and the overlay provide default layout, surface, and transition classes. Supplying a custom content `class` replaces the default content class set.
- **Focus Management**: Keeps focus within the active modal surface and restores focus when it closes.
- **Scroll Lock & Portal Rendering**: Portals modal content and prevents background body scrolling while open.
- **Dismissal & Overlay Control**: Configure outside/Escape dismissal and optional overlay rendering.
- **Dialog Semantics**: Supports `ariaLabel`, `ariaLabelledBy`, and the dialog role on the content surface.

## Playground

<Playground
  controls={[
    { kind: 'input', prop: 'ariaLabel', label: 'Accessible name', defaultValue: 'Project details' },
    { kind: 'switch', prop: 'dismissible', label: 'Dismissible', defaultValue: true },
    { kind: 'switch', prop: 'overlay', label: 'Overlay', defaultValue: true },
  ]}
>
  {(props) => {
    return (
      <Modal dismissible={props.dismissible}>
        <Modal.Trigger as={Button} variant="outline">
          Open Modal
        </Modal.Trigger>
        <Modal.Content
          overlay={props.overlay}
          ariaLabel={props.ariaLabel}
          contentRender={(context) => (
            <div class="p-5 gap-4 grid">
              <div>
                <h4 class="text-sm text-foreground font-semibold">Custom Modal Surface</h4>
                <p class="text-xs text-muted-foreground leading-relaxed mt-1">
                  Modal provides focus management, scroll locking, dismissal, and overridable
                  default surface styles.
                </p>
              </div>
              <div class="flex gap-2 justify-end">
                <Button size="sm" variant="outline" onClick={context.close}>
                  Got it
                </Button>
              </div>
            </div>
          )}
        />
      </Modal>
    )
  }}
</Playground>

## Anatomy

```text
Modal (state provider)
├── Modal.Trigger
└── Modal.Content (portal)
    ├── overlay (optional)
    └── content
        └── contentRender ({ close })
```

- `Modal`: Coordinates open state and dismissal behavior; it does not render a DOM root of its own.
- `Modal.Trigger`: Polymorphic element that opens the modal.
- `Modal.Content`: Portaled overlay and content surface.

## Usage

### Composition

`Modal` is a state provider, not a DOM root. Compose `Modal.Trigger` with `Modal.Content`; the content is portaled and owns the optional overlay plus focus and scroll behavior. Use `Dialog` or `Sheet` when their structured shell is a closer fit.

<Preview path="./modal-composition" />

### State, styling, and exit lifecycle

Use `open` with `onOpenChange` for controlled visibility. `Modal.Content` applies default popup classes only when its `class` is omitted; provide a class when the application owns the content animation. `onExitComplete` waits for the registered overlay and content exit presence to finish.

<Preview path="./modal-lifecycle" />

### Keyboard interaction

| Key                    | Description                                        |
| ---------------------- | -------------------------------------------------- |
| <kbd>Escape</kbd>      | Requests dismissal when `dismissible={true}`.      |
| <kbd>Tab</kbd>         | Moves focus forward within the modal focus scope.  |
| <kbd>Shift + Tab</kbd> | Moves focus backward within the modal focus scope. |

## Examples

### Basic composition

<Preview path="./basic" />

### Controlled state

<Preview path="./controlled" />

### Without backdrop

<Preview path="./no-overlay" />

### Exit lifecycle

<Preview path="./exit-lifecycle" />
