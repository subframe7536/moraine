---
title: Modal
description: Low-level modal primitives for composing custom dialog surfaces.
sidebar:
  order: 1
search:
  tags: [modal, primitive, overlay, focus trap]
---

Use `Dialog` or `Sheet` for their standard layouts. Use the Modal primitives when a custom modal
surface cannot be expressed by either component.

## Import

```tsx
import { Modal } from 'moraine'
```

## Composition

`Modal` owns state, scroll locking, focus containment, dismissal, and nested-overlay behavior.
`Modal.Trigger` renders a native button by default, and accepts `as` for a different intrinsic root
or a component such as `Button`. `Modal.Content` owns the portal and renders the overlay and content
as sibling surfaces. Set `overlay` to render the optional backdrop.

When `Modal.Content` does not receive a `class`, it uses the default popup transition classes
(`data-expanded:animate-popup-in` and `data-closed:animate-popup-out`). Passing `class` replaces
the default content classes completely, so custom surfaces can provide their own layout and motion.

```text
Modal
├── Modal.Trigger
└── Modal.Content (portal)
    ├── overlay (optional)
    └── content
        └── custom content
```

Provide `ariaLabel`, `ariaLabelledBy`, or another accessible naming strategy for every modal.

## Examples

### Playground

Use primitive controls for the accessible name, backdrop, and dismissal behavior; use the following
examples for state ownership and composition.

<Example
  path="./playground"
  playground
  controls={[
    { kind: 'input', prop: 'ariaLabel', label: 'Accessible name', defaultValue: 'Project details' },
    { kind: 'switch', prop: 'dismissible', label: 'Dismissible', defaultValue: true },
    { kind: 'switch', prop: 'overlay', label: 'Overlay', defaultValue: true },
  ]}
/>

### Basic Composition

Compose a custom modal surface with the default uncontrolled state.

<Example path="./basic" />

### Controlled State

Own the open state when another part of the interface must coordinate the modal.

<Example path="./controlled" />

### Without Backdrop

Omit the optional backdrop when the surrounding surface already provides context.

<Example path="./no-overlay" />
