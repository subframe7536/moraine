---
title: DropdownMenu
description: Open a compact action menu from a visible trigger.
api:
  path: src/overlays/dropdown-menu/dropdown-menu
  parts: [Trigger, Content]
sidebar:
  order: 6
search:
  tags: [actions, menu, submenu, dropdown, dropdown menu, popover menu]
---

import { Button, DropdownMenu } from '@src'

## Import

```tsx
import { DropdownMenu } from 'moraine'
import type { DropdownMenuT } from 'moraine'
```

Use DropdownMenu for a compact set of actions opened from a visible trigger. Use [Select](/select) for choosing a form value.

## Playground

<Playground
  controls={[
    {
      kind: 'select',
      prop: 'size',
      label: 'Size',
      defaultValue: 'md',
      options: [
        { label: 'Small', value: 'sm' },
        { label: 'Medium', value: 'md' },
        { label: 'Large', value: 'lg' },
      ],
    },
    { kind: 'switch', prop: 'disabled', label: 'Disabled', defaultValue: false },
  ]}
>
  {(props) => (
    <DropdownMenu disabled={props.disabled}>
      <DropdownMenu.Trigger as={Button} variant="outline" trailing="i-lucide:chevron-down">
        Account Actions
      </DropdownMenu.Trigger>
      <DropdownMenu.Content
        items={[
          {
            type: 'group',
            label: 'Account',
            children: [
              { label: 'Profile', icon: 'i-lucide:user' },
              { label: 'Billing', icon: 'i-lucide:credit-card' },
              { label: 'Settings', icon: 'i-lucide:settings', kbds: ['⌘', 'S'] },
            ],
          },
          {
            type: 'group',
            children: [{ label: 'Sign out', icon: 'i-lucide:log-out', variant: 'destructive' }],
          },
        ]}
        size={props.size}
      />
    </DropdownMenu>
  )}
</Playground>

## Anatomy

`DropdownMenu` has no root DOM element. Trigger opens the portaled Content, which renders groups and items. Put native surface attributes on Content. Root `classes` and `styles` set defaults for this menu; Content can override its slots, including nested submenu layers. See [Styling](/styling/overrides#override-component-styles).

```text
Root (context only)
├── Trigger
└── content (floating portal)
    └── group (×n)
        ├── groupLabel (optional)
        ├── separator (optional)
        └── item (×n)
            ├── itemLeading
            ├── itemWrapper
            │   ├── itemLabel
            │   └── itemDescription
            ├── itemTrailing
            ├── itemKbds
            ├── itemIndicator
            └── itemSubIndicator
```

## Usage

### Trigger and items

Use `DropdownMenu.Trigger` to compose the click, keyboard, and ARIA behavior with your native attributes. Describe each action in the item model; groups and separators organize that model without adding application state.

<Preview path="./trigger-items" />

### Stateful and nested items

Checkbox and radio-like menu items receive their checked or selected state from your application through their callbacks. Nested children create submenus. Disabled items remain visible but cannot be activated.

<Preview path="./stateful-items" />

### Keyboard interaction

| Key                                                        | Description                                           |
| ---------------------------------------------------------- | ----------------------------------------------------- |
| <kbd>Enter</kbd> / <kbd>Space</kbd> / <kbd>ArrowDown</kbd> | Opens the menu from an eligible trigger interaction.  |
| <kbd>ArrowDown</kbd> / <kbd>ArrowUp</kbd>                  | Moves the active item through available menu entries. |
| <kbd>ArrowRight</kbd>                                      | Opens an available submenu from its parent item.      |
| <kbd>ArrowLeft</kbd>                                       | Returns from a submenu to its parent menu.            |
| <kbd>Enter</kbd> / <kbd>Space</kbd>                        | Activates the current menu item.                      |
| <kbd>Escape</kbd>                                          | Closes the current menu layer.                        |

## Examples

### Workspace view settings

Toggle visible fields, include archived projects, and choose a sort order. The preview updates from application-owned state; saving a shared view is disabled for viewers.

<Preview path="./workspace-view" />

### Basic menu

<Preview path="./basic" />

### Checkbox and radio items

<Preview path="./checkbox-radio" />

### Submenus

<Preview path="./submenus" />

### Shortcuts and disabled items

<Preview path="./shortcuts-disabled" />
