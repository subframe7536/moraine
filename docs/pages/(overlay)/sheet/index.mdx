---
title: Sheet
description: Slide-in panel overlay from any screen edge with structured content slots.
sidebar:
  order: 5
search:
  tags: [drawer, panel, overlay, mobile]
---

## Import

```tsx
import { Sheet } from 'moraine'
```

## Slot Structure

Polymorphic trigger root with optional backdrop and a sliding panel with title, body, footer, and close slots.

```text
trigger
├── overlay (optional)
└── content (portal)
    ├── header (optional)
    │   ├── wrapper (optional)
    │   │   ├── title (optional)
    │   │   └── description (optional)
    │   ├── actions (optional)
    │   └── close (optional)
    ├── body (optional)
    └── footer (optional)
```

## Examples

### Playground

Adjust common primitive props while keeping the focused scenarios below for composition and controlled state.

<Example path="./variants" playground controls={[
{ kind: 'switch', prop: 'dismissible', label: 'Dismissible', defaultValue: true },
{ kind: 'switch', prop: 'overlay', label: 'Overlay', defaultValue: true },
]}/>

### Variants

Inset layout with custom close content or hidden close control.

<Example path="./variants" />

### Sides

Open sheet from each side with shared shell slots.

<Example path="./sides" />

### Dismiss Control

Prevent close on outside interaction and Escape while counting attempts.

<Example path="./dismiss-control" />
