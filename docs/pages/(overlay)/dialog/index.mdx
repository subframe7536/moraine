---
title: Dialog
description: Modal dialog with structured slots, backdrop overlay, and dismissal control.
sidebar:
  order: 4
search:
  tags: [modal, overlay, confirmation, focus trap]
---

## Import

```tsx
import { Dialog } from 'moraine'
```

## Slot Structure

Polymorphic trigger root with optional backdrop and a floating panel with title, body, and footer slots.

```text
trigger
├── overlay (optional)
└── content (portal)
    ├── header (optional)
    │   ├── wrapper (optional)
    │   │   ├── title (optional)
    │   │   └── description (optional)
    │   └── close (optional)
    ├── body (optional)
    └── footer (optional)
```

## Examples

### Playground

Adjust common primitive props while keeping the focused scenarios below for composition and controlled state.

<Example
  path="./playground"
  playground
  controls={[
    { kind: 'input', prop: 'title', label: 'Title', defaultValue: 'Delete deployment' },
    { kind: 'switch', prop: 'fullscreen', label: 'Fullscreen', defaultValue: false },
    { kind: 'switch', prop: 'overlay', label: 'Overlay', defaultValue: true },
  ]}
/>

### Default Shell

Header, description, actions, body, and footer slots.

<Example path="./default-shell" />

### Variants

`close` supports default button, hidden, and custom JSX content.

<Example path="./variants" />

### Long Content + Dismissible Control

Long body content with a prevent-close callback when dismiss is disabled.

By default, long content scrolls inside the dialog body. Set `scrollable` to `true` to scroll the complete dialog panel with the overlay.

<Example path="./long-content-dismissible-control" />
