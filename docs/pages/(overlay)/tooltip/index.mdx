---
title: Tooltip
description: Hover-triggered informational overlay anchored to a trigger element.
sidebar:
  order: 3
search:
  tags: [hint, hover, label, floating]
---

import { Button, Tooltip } from '@src'

## Import

```tsx
import { Tooltip } from 'moraine'
```

## Playground

<Playground
  controls={[
    { kind: 'input', prop: 'text', label: 'Tooltip text', defaultValue: 'Create new document' },
    {
      kind: 'select',
      prop: 'placement',
      label: 'Placement',
      defaultValue: 'top',
      options: [
        { label: 'Top', value: 'top' },
        { label: 'Bottom', value: 'bottom' },
        { label: 'Left', value: 'left' },
        { label: 'Right', value: 'right' },
      ],
    },
    { kind: 'switch', prop: 'disabled', label: 'Disabled', defaultValue: false },
    { kind: 'switch', prop: 'invert', label: 'Invert colors', defaultValue: false },
  ]}
>
  {(props) => {
    return (
      <div class="p-6 flex items-center justify-center">
        <Tooltip
          text={props.text}
          kbds={['⌘', 'N']}
          placement={props.placement}
          disabled={props.disabled}
          invert={props.invert}
        >
          {(triggerProps) => (
            <Button {...triggerProps} variant="outline" leading="i-lucide:file-plus">
              Hover me
            </Button>
          )}
        </Tooltip>
      </div>
    )
  }}
</Playground>

## Slot Structure

Polymorphic trigger root with a floating content portal containing text and optional keyboard hints.

```text
trigger
└── content (portal)
    ├── text (optional)
    └── kbds (optional)
        └── kbd (×n)
```

## Examples

### Placements

Tooltip positioned on each side.

<Example path="./placements" />

### Trigger Types

Tooltip on buttons and inline text.

<Example path="./trigger-types" />

### Keyboard Shortcuts

Display keyboard shortcut hints alongside tooltip text.

<Example path="./keyboard-shortcuts" />

### Text Only vs Shortcuts Only

Content variations.

<Example path="./text-only-vs-shortcuts-only" />
