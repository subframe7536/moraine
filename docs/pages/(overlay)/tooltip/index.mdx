---
title: Tooltip
description: Show supplemental, non-interactive information on hover or focus.
api:
  path: src/overlay/tooltip/tooltip
  parts: [Trigger, Content]
sidebar:
  order: 3
search:
  tags: [hint, hover, label, floating, tooltip, shortcut hint, popover hint]
---

import { Button, Tooltip } from '@src'

## Import

```tsx
import { Tooltip } from 'moraine'
import type { TooltipT } from 'moraine'
```

Use Tooltip for supplemental information on hover or focus. The trigger must still have its own accessible name when the control needs one.

## Playground

<Playground
  controls={[
    { kind: 'input', prop: 'text', label: 'Tooltip text', defaultValue: 'Create new document' },
    {
      kind: 'select',
      prop: 'placement',
      label: 'Placement',
      defaultValue: 'top',
      options: [
        { label: 'Top', value: 'top' },
        { label: 'Bottom', value: 'bottom' },
        { label: 'Left', value: 'left' },
        { label: 'Right', value: 'right' },
      ],
    },
    {
      kind: 'select',
      prop: 'align',
      label: 'Align',
      defaultValue: 'center',
      options: [
        { label: 'Start', value: 'start' },
        { label: 'Center', value: 'center' },
        { label: 'End', value: 'end' },
      ],
    },
    { kind: 'switch', prop: 'disabled', label: 'Disabled', defaultValue: false },
    { kind: 'switch', prop: 'invert', label: 'Invert colors', defaultValue: false },
  ]}
>
  {(props) => (
    <div class="p-6 flex items-center justify-center">
      <Tooltip placement={props.placement} align={props.align} disabled={props.disabled}>
        <Tooltip.Trigger as={Button} variant="outline" leading="i-lucide:file-plus">
          Hover me
        </Tooltip.Trigger>
        <Tooltip.Content text={props.text} kbds={['⌘', 'N']} invert={props.invert} />
      </Tooltip>
    </div>
  )}
</Playground>

## Anatomy

```text
Tooltip (state only; no DOM)
├── trigger (Tooltip.Trigger)
└── positioner (internal; portaled while mounted)
    └── content (Tooltip.Content)
        ├── text (optional)
        └── kbds (KbdGroup, optional)
            └── item (<kbd>, ×n)
```

String content uses the `text` slot; JSX content renders directly inside Content.

Put native surface attributes on Content. Root `classes` and `styles` set defaults for this Tooltip; Content can override its own slots. See [Styling](/styling/customization#style-overrides).

The positioning wrapper is internal. You cannot style it through Tooltip slots or Theme overrides.

## Usage

Keep tooltip content supplemental and non-interactive. Its text should not be the only accessible name for an otherwise unlabeled control.

### Supplemental information

Use tooltips for non-interactive hints that supplement an already labeled trigger. Do not use tooltip text as the sole accessible name of an icon-only control.

<Preview path="./hints-usage" />

### Triggering and shortcuts

Tooltip opens from pointer hover and focus. `kbds` displays shortcut notation alongside the hint; it does not register that shortcut. Set `kbdVariant` on `Tooltip.Content` to `default`, `outline`, or `invert`. When omitted, keycaps follow the content's `invert` setting.

<Preview path="./shortcuts-usage" />

### Keyboard interaction

Focusing a focusable trigger can display the tooltip. Escape dismisses an open tooltip according to the shared tooltip overlay behavior; focus remains on the trigger rather than moving into the tooltip content.

## Examples

### Trigger types

<Preview path="./trigger-types" />

### Keyboard shortcuts

<Preview path="./keyboard-shortcuts" />
