---
title: Toast
description: Temporary notification messages with asynchronous promise lifecycles, action buttons, progress bars, and position anchoring powered by solid-toaster.
sidebar:
  order: 8
search:
  tags: [notification, message, feedback, promise, toast, alert, toaster]
componentKey: toast
category: overlays
upstreamHref: https://github.com/subframe7536/solid-toaster
---

import 'solid-toaster/style.css'
import { Button } from '@src'
import { toast } from 'solid-toaster'

## Import

Install `solid-toaster`:

<CodeTabs package="solid-toaster" />

Then import styles and mount a `Toaster` instance at your application root:

```tsx
import 'solid-toaster/style.css'

import { Button } from 'moraine'
import { Toaster, toast } from 'solid-toaster'

export default function App() {
  return (
    <>
      <Button onClick={() => toast.success('Changes saved!')}>Save</Button>
      <Toaster />
    </>
  )
}
```

## Playground

Interactive component preview with configurable props.

<Playground
  controls={[
    {
      kind: 'input',
      prop: 'message',
      label: 'Message',
      defaultValue: 'Changes saved successfully',
    },
    {
      kind: 'select',
      prop: 'tone',
      label: 'Tone',
      defaultValue: 'success',
      options: [
        { label: 'Success', value: 'success' },
        { label: 'Warning', value: 'warning' },
        { label: 'Error', value: 'error' },
        { label: 'Info', value: 'info' },
      ],
    },
  ]}
>
  {(props) => {
    const showToast = () => {
      const message = props.message
      switch (props.tone) {
        case 'warning':
          toast.warning(message)
          break
        case 'error':
          toast.error(message)
          break
        case 'info':
          toast(message)
          break
        default:
          toast.success(message)
      }
    }

    return (
      <Button onClick={showToast} variant="outline" leading="i-lucide:bell">
        Trigger Toast Notification
      </Button>
    )

}}

</Playground>

## Features

- **Asynchronous Promise Lifecycle**: `toast.promise(promise, { loading, success, error })` seamlessly updates message tone and icons in-place as network calls resolve.
- **Multiple Semantic Tones**: Dedicated methods for `toast.success()`, `toast.error()`, `toast.warning()`, and `toast.info()`.
- **Scoped Multi-Toaster Instances**: Target specific viewport regions or modal contexts using `toasterId`.
- **Dismissal & Duration Controls**: Custom per-toast timeouts, progress timers, and manual `toast.dismiss()` triggers.
- **Accessible ARIA Live Regions**: Announces messages via `role="status"` and `aria-live="polite"` or `aria-live="assertive"`.

## Anatomy

Hierarchical slot and element structure:

```text
Toaster (viewport container)
└── Toast (floating card ×n)
    ├── icon (status indicator icon)
    ├── content
    │   ├── title (primary message string)
    │   └── description (optional subtext)
    ├── action (optional action button)
    └── close (optional dismiss button)
```

- `Toaster`: Mounts at top-level root managing stacking, gaps, and z-index.
- `Toast`: Individual animated notification card.

## Examples

### Basic Toasts

Send success, error, warning, and info notification toasts to the global toaster.

<Example path="./basic-toasts" />

### Promise + Scoped Instances

Track async git push operations with `toast.promise` and target scoped sub-toasters.

<Example path="./promise-scoped-instances" />

### Duration and Dismissal

Customize display duration in milliseconds, or dismiss all notifications programmatically.

<Example path="./duration" />
