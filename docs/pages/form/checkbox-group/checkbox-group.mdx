---
header: true
---

import { DemoCheckboxGroupVariants } from './variants?example'
import { DemoCheckboxGroupSizes } from './sizes?example'
import { DemoCheckboxGroupOrientation } from './orientation?example'
import { DemoCheckboxGroupIndicator } from './indicator?example'
import { DemoCheckboxGroupControlledDisabledItems } from './controlled-disabled-items?example'

## Import

```tsx
import { CheckboxGroup } from 'moraine'
```

## Slot Structure

Fieldset with an optional legend grouping multiple Checkbox items.

```text
root
└── fieldset
    ├── legend (optional)
    └── root (Checkbox, ×n)
```

## Examples

### Variants

List, card, and table variants with shared items data.

<DemoCheckboxGroupVariants />

### Sizes

Size scale from xs to xl in card variant.

<DemoCheckboxGroupSizes />

### Orientation

Vertical and horizontal layouts with the same item set.

<DemoCheckboxGroupOrientation />

### Indicator

Start/end/hidden indicator positions in list layout.

<DemoCheckboxGroupIndicator />

### Controlled + Disabled Items

Controlled selected values with per-item disabled state.

<DemoCheckboxGroupControlledDisabledItems />
