---
title: Pagination
description: Page navigation with sibling windows, optional previous and next controls, link destinations, and current-page metadata.
sidebar:
  order: 4
search:
  tags: [pages, previous, next, navigation, pagination, table paging]
---

import { Pagination } from '@src'
import { createSignal } from 'solid-js'

## Import

```tsx
import { Pagination } from 'moraine'
import type { PaginationT } from 'moraine'
```

## Features

- **Sibling Window**: `siblingCount` controls how many nearby page numbers remain visible around the current page, with ellipses filling gaps.
- **Navigation Controls**: `showControls` controls previous/next buttons. The first and last page numbers remain available around the sibling window.
- **Pagination Landmark**: Uses a pagination navigation landmark and marks the active page with `aria-current="page"`.
- **Link Destinations**: Use `to(page)` to generate anchor destinations for page controls.
- **Controlled State**: `page` and `onPageChange` support application-owned page state.

## Playground

<Playground
  controls={[
    {
      kind: 'select',
      prop: 'size',
      label: 'Size',
      defaultValue: 'md',
      options: [
        { label: 'Small', value: 'sm' },
        { label: 'Medium', value: 'md' },
        { label: 'Large', value: 'lg' },
      ],
    },
    {
      kind: 'input',
      prop: 'itemsPerPage',
      label: 'Items per page',
      defaultValue: 10,
      inputType: 'number',
    },
    { kind: 'switch', prop: 'showControls', label: 'Show arrows', defaultValue: true },
  ]}
>
  {(props) => {
    const [page, setPage] = createSignal(1)
    const itemsPerPage = () => {
      const value = Number(props.itemsPerPage)
      return Number.isFinite(value) ? Math.max(1, Math.trunc(value)) : 10
    }
    const pageCount = () => Math.ceil(100 / itemsPerPage())
    const currentPage = () => Math.min(page(), pageCount())
    return (
      <div class="flex flex-col gap-3 items-center">
        <Pagination
          total={100}
          page={currentPage()}
          onPageChange={setPage}
          itemsPerPage={itemsPerPage()}
          size={props.size}
          showControls={props.showControls}
        />
        <span class="text-xs text-muted-foreground font-mono">
          Current Page: {currentPage()} of {pageCount()}
        </span>
      </div>
    )
  }}
</Playground>

## Anatomy

```text
root (pagination navigation)
└── list
    ├── item → prev (optional)
    ├── item → page link / ellipsis (×n)
    └── item → next (optional)
```

## Usage

### Current page

Use `page` with `onPageChange` for controlled navigation. `total` is the number of records, not the page count; the component derives the page count from `itemsPerPage`. The surrounding application owns data fetching and URL updates.

<Preview path="./page-state" />

### Rendering links

Use `to(page)` to render page controls as links. This example uses Solid Router's `useSearchParams` to keep the current page synchronized with destinations such as `/pagination?page=2`, including browser back and forward navigation. The application owns URL state; `to` only generates destinations.

<Preview path="./link-rendering" />

### Keyboard interaction

Pagination does not add collection-style arrow navigation. Each interactive page control uses the keyboard semantics of its rendered button, link, or custom polymorphic element.

## Examples

### Controlled page

<Preview path="./controlled" />

### Minimal pagination

<Preview path="./minimal" />
