---
title: FormField
description: Form field wrapper providing label, description, and validation message layout.
sidebar:
  order: 2
search:
  tags: [label, validation, errors, field]
---

## Import

```tsx
import { createForm, Form, FormField } from 'moraine'
```

Inside `Form`, a named field automatically reads its value and validation state from Formisch.
Top-level fields accept a string name; nested and array fields use a path such as
`['users', 0, 'email']`. Outside `Form`, the component remains a standalone layout primitive.

Pass the schema type to validate names at compile time:

```tsx
<FormField<typeof schema> name={['profile', 'email']} label="Email">
  <Input />
</FormField>
```

## Slot Structure

Label, description, and hint area above a control slot with validation feedback.

```text
root
├── wrapper
│   ├── labelWrapper (optional)
│   │   ├── label
│   │   └── hint (optional)
│   └── description (optional)
└── container
    ├── {children}
    ├── help (optional)
    └── error (optional)
```

## Examples

### Playground

Adjust common primitive props while keeping the focused scenarios below for composition and controlled state.

<Example path="./basic" playground controls={[
{ kind: 'switch', prop: 'required', label: 'Required', defaultValue: false },
{ kind: 'input', prop: 'error', label: 'Error', defaultValue: '' },
]}/>

### Basic

Label, hint, description, and help text with a single control.

<Example path="./basic" />

### With Validation

Bind to a Formisch store and show validation feedback without a render prop.

<Example path="./with-validation" />

### Horizontal Layout

Use `orientation="horizontal"` for a fixed one-row layout with a right-aligned label column occupying one quarter of the field and controls occupying the remaining three quarters. The layout stays horizontal at every width; use existing class overrides when an application needs stacking.

<Example path="./horizontal-layout" />

### Registration Form

Combine horizontal fields into a complete registration form with consistent control alignment.

<Example path="./registration-form" />

### Sizes

Preview the field typography scale from sm to lg.

<Example path="./sizes" />

### Manual Error

Force error state with a custom error message.

<Example path="./manual-error" />

### Render Context

Use render-function children to react to `error` state.

<Example path="./render-context" />

### Nested Path

Use array path names for nested form fields.

<Example path="./nested-path" />
