---
title: FormField
description: An accessible form field wrapper coordinating labels, optional hints, descriptions, required indicators, and real-time validation error feedback.
sidebar:
  order: 2
search:
  tags: [label, validation, errors, field, form control, hint, description]
---

import { FormField, Input } from '@src'

## Import

Import the component and its companion types from the package root:

```tsx
import { FormField } from 'moraine'
import type { FormFieldT } from 'moraine'
```

When used inside `Form`, a named `FormField` automatically subscribes to schema validation and errors:

```tsx
<FormField<typeof schema> name={['profile', 'email']} label="Email Address">
  <Input placeholder="you@company.com" leading="i-lucide:mail" />
</FormField>
```

## Playground

Interactive component preview with configurable props.

<Playground
  controls={[
    { kind: 'input', prop: 'label', label: 'Label', defaultValue: 'Email address' },
    { kind: 'input', prop: 'hint', label: 'Hint', defaultValue: 'Optional' },
    {
      kind: 'input',
      prop: 'description',
      label: 'Description',
      defaultValue: "We'll send account alerts to this address.",
    },
    {
      kind: 'select',
      prop: 'size',
      label: 'Size',
      defaultValue: 'md',
      options: [
        { label: 'Small', value: 'sm' },
        { label: 'Medium', value: 'md' },
        { label: 'Large', value: 'lg' },
      ],
    },
    { kind: 'switch', prop: 'required', label: 'Required', defaultValue: false },
    { kind: 'input', prop: 'error', label: 'Error message', defaultValue: '' },
  ]}
>
  {(props) => {
    return (
      <FormField
        label={props.label}
        description={props.description}
        hint={props.hint}
        error={props.error ? props.error : undefined}
        size={props.size}
        required={props.required}
      >
        <Input placeholder="you@example.com" leading="i-lucide:mail" />
      </FormField>
    )
  }}
</Playground>

## Features

- **Automated ARIA Association**: Automatically links `<label>`, helper description (`aria-describedby`), and error messages (`aria-errormessage`) with the child control using deterministic IDs.
- **Formisch Schema Integration**: Binds nested or flat path names (`['users', 0, 'email']`) with type-safe schema validation.
- **Vertical & Horizontal Layouts**: Switch to `orientation="horizontal"` for 1-to-3 grid columns common in desktop settings and admin consoles.
- **Render Context & Error Reactivity**: Child function render prop receives `{ invalid, error, size, disabled, required }` to dynamically tailor child components.
- **Granular Slots**: Supports separate slots for `label`, `hint`, `description`, `help`, and `error`.

## Anatomy

Hierarchical slot and element structure:

```text
root
├── wrapper (label & description header)
│   ├── labelWrapper
│   │   ├── label (form control label)
│   │   ├── required (asterisk indicator)
│   │   └── hint (optional badge/subtext)
│   └── description (helper text)
└── container (control & feedback area)
    ├── {children} (input, select, textarea, etc.)
    ├── help (supplemental help note)
    └── error (validation error message)
```

- `root`: Field wrapper applying size, invalid state, and layout classes.
- `wrapper`: Top section holding the title, hint, and description.
- `container`: Bottom or right section holding the input control and error feedback.
- `error`: Rendered conditionally when validation errors exist, automatically setting `role="alert"`.

## Examples

### Basic

Complete vertical field layout with label, optional hint, helper description, input control, and help footer.

<Example path="./basic" />

### With Validation

Integrates with Formisch schema stores to display inline error validation messages automatically upon blur or submit.

<Example path="./with-validation" />

### Horizontal Layout

Uses `orientation="horizontal"` to place the label on the left column and controls on the right, perfect for account settings and profile forms.

<Example path="./horizontal-layout" />

### Registration Form

Compose multiple fields into a cohesive multi-input form layout with unified spacing and alignment.

<Example path="./registration-form" />

### Sizes

Preview the typography and spacing scale across `sm`, `md`, and `lg`.

<Example path="./sizes" />

### Manual Error

Explicitly set an `error` string or boolean to force an error state without schema validation.

<Example path="./manual-error" />

### Render Context

Pass a render function as children to receive live field state `{ error, invalid, size }` and adapt your custom controls.

<Example path="./render-context" />

### Nested Path

Type-safe schema binding with array path notation for deeply nested objects and arrays.

<Example path="./nested-path" />

## Accessibility

- Automatically injects `id`, `aria-labelledby`, `aria-describedby`, and `aria-invalid` onto supported child controls.
- Renders error messages with `role="alert"` so screen readers immediately announce validation issues.
