---
title: Textarea
description: Multi-line text input with autoresize support and form field integration.
sidebar:
  order: 4
search:
  tags: [multiline, text field, autoresize, form control]
---

## Import

```tsx
import { Textarea } from 'moraine'
```

## Slot Structure

Multi-line text input with optional header and footer slots.

```text
root
├── header (optional)
├── input
└── footer (optional)
```

## Examples

### Playground

Adjust common primitive props while keeping the focused scenarios below for composition and controlled state.

<Example
  path="./playground"
  playground
  controls={[
    {
      kind: 'input',
      prop: 'placeholder',
      label: 'Placeholder',
      defaultValue: 'Write a comment or description...',
    },
    {
      kind: 'select',
      prop: 'variant',
      label: 'Variant',
      defaultValue: 'outline',
      options: [
        { label: 'Outline', value: 'outline' },
        { label: 'Subtle', value: 'subtle' },
        { label: 'Ghost', value: 'ghost' },
        { label: 'None', value: 'none' },
      ],
    },
    {
      kind: 'select',
      prop: 'size',
      label: 'Size',
      defaultValue: 'md',
      options: [
        { label: 'Small', value: 'sm' },
        { label: 'Medium', value: 'md' },
        { label: 'Large', value: 'lg' },
      ],
    },
    { kind: 'switch', prop: 'disabled', label: 'Disabled', defaultValue: false },
    { kind: 'switch', prop: 'autoResize', label: 'Auto-resize', defaultValue: true },
  ]}
/>

### Variants

Outline/subtle/ghost/none visual variants.

<Example path="./variants" />

### Sizes

Textarea size scale from sm to lg.

<Example path="./sizes" />

### Autoresize

Autoresize growth with maxrows.

<Example path="./autoresize" />

### Header + Footer

Build composer-like surfaces using header/footer slots.

<Example path="./header-footer" />
