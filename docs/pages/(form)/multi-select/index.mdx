---
title: MultiSelect
description: Collection-backed multiple selection with tags, optional search, and explicit item creation.
sidebar:
  order: 13
search:
  tags: [combobox, multiple selection, items, multi-select, tags]
---

import { MultiSelect } from '@src'

## Import

```tsx
import { MultiSelect } from 'moraine'
import type { MultiSelectT } from 'moraine'
```

## Features

- **Collection-backed values**: Every committed value resolves through a canonical `TItem` source.
- **Stable tag field**: One input owns focus, keyboard behavior, query, and combobox ARIA.
- **Optional search and creation**: `search` filters; `createItem` constructs a `TItem` for free-form input.
- **Unified tokenization**: Separators and paste reuse the same collection-aware creation path.
- **Separate limits**: `maxCount` limits values; `maxTagCount` only changes presentation.

## Playground

<Playground
  controls={[
    {
      kind: 'input',
      prop: 'placeholder',
      label: 'Placeholder',
      defaultValue: 'Select topics...',
    },
    {
      kind: 'select',
      prop: 'variant',
      label: 'Variant',
      defaultValue: 'outline',
      options: [
        { label: 'Outline', value: 'outline' },
        { label: 'Subtle', value: 'subtle' },
        { label: 'Ghost', value: 'ghost' },
        { label: 'None', value: 'none' },
      ],
    },
    {
      kind: 'select',
      prop: 'size',
      label: 'Size',
      defaultValue: 'md',
      options: [
        { label: 'Small', value: 'sm' },
        { label: 'Medium', value: 'md' },
        { label: 'Large', value: 'lg' },
      ],
    },
    { kind: 'switch', prop: 'search', label: 'Search', defaultValue: false },
    {
      kind: 'switch',
      prop: 'openOnControlClick',
      label: 'Open on control click',
      defaultValue: true,
    },
    { kind: 'switch', prop: 'allowClear', label: 'Clearable', defaultValue: true },
    { kind: 'switch', prop: 'disabled', label: 'Disabled', defaultValue: false },
    { kind: 'switch', prop: 'readOnly', label: 'Read-only', defaultValue: false },
    { kind: 'switch', prop: 'loading', label: 'Loading', defaultValue: false },
  ]}
>
  {(props) => (
    <MultiSelect
      items={[
        { label: 'Design', value: 'design', icon: 'i-lucide:palette' },
        { label: 'Development', value: 'dev', icon: 'i-lucide:code' },
        { label: 'Marketing', value: 'marketing', icon: 'i-lucide:megaphone' },
        { label: 'Product', value: 'product', icon: 'i-lucide:layers' },
        { label: 'Research', value: 'research', icon: 'i-lucide:search' },
      ]}
      defaultValue={['design']}
      placeholder={props.placeholder}
      variant={props.variant}
      size={props.size}
      search={props.search}
      openOnControlClick={props.openOnControlClick}
      allowClear={props.allowClear}
      disabled={props.disabled}
      readOnly={props.readOnly}
      loading={props.loading}
      class="max-w-xl"
    />
  )}
</Playground>

## Anatomy

```text
control
├── leading (optional)
├── tagsContainer
│   ├── tag
│   │   ├── tagLabel
│   │   └── tagRemove
│   ├── tagOverflow (optional)
│   └── input
├── clear (optional)
└── trigger
```

The trailing trigger is always a real secondary button and is never replaced by clear or loading.
By default, control clicks do not open the popup. Set `openOnControlClick={true}` to allow
ordinary control clicks to open the popup; typing and Arrow keys still open normally.

## Usage

### Values and search

<Preview path="./values-and-search" />

Without `search`, the single physical input is read-only and printable keys use collection
typeahead. `search` makes it editable and filters the collection. Query state stays separate from
the committed `TItem['value'][]`.

### Create collection items

<Preview path="./create-new-tags" />

Providing `createItem` implicitly enables the editable search input, even with `search={false}`.
It converts normalized text into a valid `TItem`; MultiSelect then commits `TItem.value`. Exact
label or value matches reuse existing items before calling `createItem`.

### Free-form tags

<Preview path="./free-form-tags" />

With no predefined items, `createItem` and `tokenSeparators` provide free-form tag entry without a
separate component or selection model. Separator typing and pasted tokens use the same commit path
as Enter.

### Large item sets

<Preview path="./large-collections" />

## Examples

### Selection and display limits

`maxCount` is a behavioral selection limit. `maxTagCount` is visual only; hidden selections remain
committed, submitted, and reachable through the collection popup.

<Preview path="./max-count-max-tag-count" />

### Virtualization

<Preview path="./virtualization" />

### Form integration

<Preview path="./form-integration" />
