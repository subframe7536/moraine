---
title: Switch
description: Toggle switch control with icon slots and loading state.
sidebar:
  order: 9
search:
  tags: [toggle, boolean, checked, setting]
---

import { Switch } from '@src'

## Import

```tsx
import { Switch } from 'moraine'
```

## Playground

<Playground
  controls={[
    { kind: 'input', prop: 'label', label: 'Label', defaultValue: 'Email alerts' },
    {
      kind: 'select',
      prop: 'size',
      label: 'Size',
      defaultValue: 'md',
      options: [
        { label: 'Small', value: 'sm' },
        { label: 'Medium', value: 'md' },
        { label: 'Large', value: 'lg' },
      ],
    },
    { kind: 'switch', prop: 'disabled', label: 'Disabled', defaultValue: false },
    { kind: 'switch', prop: 'loading', label: 'Loading', defaultValue: false },
  ]}
>
  {(props) => {
    return (
      <Switch
        label={props.label}
        description="Receive a notification when a deployment completes."
        size={props.size}
        defaultChecked
        disabled={props.disabled}
        loading={props.loading}
      />
    )
  }}
</Playground>

## Slot Structure

Toggle track with thumb and optional label area.

```text
root
├── container
│   ├── input
│   └── track
│       └── thumb
└── wrapper (optional)
    ├── label (optional)
    └── description (optional)
```

## Examples

### Basic + Controlled

Uncontrolled and controlled switch with icon slots.

<Example path="./basic-controlled" />

### Variants

Loading, disabled, and explicit icon combinations.

<Example path="./variants" />

### Sizes

Switch size scale from sm to lg.

<Example path="./sizes" />

### Custom True/False Values

Map checked state to domain values instead of boolean.

<Example path="./custom-true-false-values" />
