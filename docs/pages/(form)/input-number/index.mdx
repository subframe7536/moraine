---
title: InputNumber
description: A numeric spinbutton input with boundary clamping, configurable step intervals, press-and-hold repeat, and horizontal or vertical stepper layouts.
sidebar:
  order: 5
search:
  tags: [number, stepper, spinbutton, numeric, counter, quantity]
---

import { InputNumber } from '@src'
import { createSignal } from 'solid-js'

## Import

```tsx
import { InputNumber } from 'moraine'
import type { InputNumberT } from 'moraine'
```

## Playground

<Playground
  controls={[
    {
      kind: 'select',
      prop: 'variant',
      label: 'Variant',
      defaultValue: 'outline',
      options: [
        { label: 'Outline', value: 'outline' },
        { label: 'Subtle', value: 'subtle' },
        { label: 'Ghost', value: 'ghost' },
        { label: 'None', value: 'none' },
      ],
    },
    {
      kind: 'select',
      prop: 'size',
      label: 'Size',
      defaultValue: 'md',
      options: [
        { label: 'Small', value: 'sm' },
        { label: 'Medium', value: 'md' },
        { label: 'Large', value: 'lg' },
      ],
    },
    {
      kind: 'select',
      prop: 'orientation',
      label: 'Orientation',
      defaultValue: 'horizontal',
      options: [
        { label: 'Horizontal', value: 'horizontal' },
        { label: 'Vertical', value: 'vertical' },
      ],
    },
    { kind: 'switch', prop: 'disabled', label: 'Disabled', defaultValue: false },
    { kind: 'switch', prop: 'increment', label: 'Increment control', defaultValue: true },
  ]}
>
  {(props) => {
    return (
      <div class="max-w-full w-64">
        <InputNumber
          variant={props.variant}
          size={props.size}
          orientation={props.orientation}
          disabled={props.disabled}
          increment={props.increment}
          minValue={0}
          maxValue={100}
        />
      </div>
    )
  }}
</Playground>

## Features

- **Boundary Clamping**: `minValue` and `maxValue` constrain committed values.
- **Decimal-Safe Stepping**: Increment and decrement operations use `step` without accumulating common floating-point artifacts.
- **Horizontal & Vertical Controls**: Place decrement/increment buttons on the sides or stack them in a right-hand rail.
- **Press-and-Hold Repeat**: Hold a step button to repeat after `repeatDelayMs` at the configured `repeatIntervalMs`; disable it with `holdRepeat={false}`.
- **Spinbutton Semantics**: The input exposes spinbutton value metadata such as `aria-valuenow`, `aria-valuemin`, and `aria-valuemax` where applicable.

## Anatomy

### Horizontal layout

```text
root
├── decrement
├── input
└── increment
```

### Vertical layout

```text
root
├── input
└── controls
    ├── increment
    └── decrement
```

- `root`: Container applying variant styling and focus state.
- `input`: Numeric text entry and spinbutton keyboard target.
- `increment` / `decrement`: Step buttons with optional press-and-hold repeat.

## Examples

### Variants

Compare `outline`, `subtle`, `ghost`, and `none` styling options.

<Example path="./variants" />

### Sizes

Size scaling across `sm`, `md`, and `lg`.

<Example path="./sizes" />

### Orientations

Switch between side-by-side and stacked step controls.

<Example path="./orientations" />

### Controlled

Bind the numeric value to a reactive signal.

<Example path="./controlled" />

### Disabled

Disable text entry and step controls together.

<Example path="./disabled" />

### Long Press

Hold the increment or decrement button to repeat stepping. Configure or disable the behavior with the hold-repeat props.

<Example path="./long-press" />

### Min / Max / Step

Combine bounds with a custom step interval.

<Example path="./min-max-step" />

## Keyboard Interactions

| Key                       | Description                                              |
| ------------------------- | -------------------------------------------------------- |
| <kbd>ArrowUp</kbd>        | Increments the value by `step`.                          |
| <kbd>ArrowDown</kbd>      | Decrements the value by `step`.                          |
| <kbd>PageUp</kbd>         | Increments by `largeStep` (defaults to `step * 10`).     |
| <kbd>PageDown</kbd>       | Decrements by `largeStep` (defaults to `step * 10`).     |
| <kbd>Home</kbd>           | Sets the value to `minValue` when a minimum is defined.  |
| <kbd>End</kbd>            | Sets the value to `maxValue` when a maximum is defined.  |
| <kbd>Enter</kbd>          | Commits the current text input as a numeric value.       |
