---
title: Separator
description: Visual divider with configurable orientation, style, and border type.
sidebar:
  order: 9
search:
  tags: [divider, horizontal, vertical, rule]
---

## Import

```tsx
import { Separator } from 'moraine'
```

## Structure

The separator renders one root element that acts as the horizontal or vertical rule.

```text
root
```

## Examples

### Playground

Adjust common primitive props while keeping the focused scenarios below for composition and controlled state.

<Example
  path="./playground"
  playground
  controls={[
    {
      kind: 'select',
      prop: 'size',
      label: 'Size',
      defaultValue: 'sm',
      options: [
        { label: 'Small', value: 'sm' },
        { label: 'Medium', value: 'md' },
        { label: 'Large', value: 'lg' },
      ],
    },
    {
      kind: 'select',
      prop: 'type',
      label: 'Border Style',
      defaultValue: 'solid',
      options: [
        { label: 'Solid', value: 'solid' },
        { label: 'Dashed', value: 'dashed' },
        { label: 'Dotted', value: 'dotted' },
      ],
    },
  ]}
/>

### Sizes

Border thickness variants.

<Example path="./sizes" />

### Types

Solid, dashed, and dotted divider styles.

<Example path="./types" />

### Vertical

Vertical separators in flexible layouts.

<Example path="./orientation" />
