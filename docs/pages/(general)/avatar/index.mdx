---
title: Avatar
description: An avatar with image preloading, fallback initials or icons, status metadata, badges, and consistent size variants.
sidebar:
  order: 4
search:
  tags: [profile, image, fallback, user, avatar, initials, status dot]
---

import { Avatar } from '@src'

## Import

```tsx
import { Avatar } from 'moraine'
import type { AvatarT } from 'moraine'
```

## Playground

<Playground
  controls={[
    { kind: 'input', prop: 'alt', label: 'Alternative text', defaultValue: 'Sarah Connor' },
    { kind: 'input', prop: 'text', label: 'Fallback text', defaultValue: 'SC' },
    {
      kind: 'select', prop: 'size', label: 'Size', defaultValue: 'md',
      options: [
        { label: 'Small', value: 'sm' },
        { label: 'Medium', value: 'md' },
        { label: 'Large', value: 'lg' },
      ],
    },
  ]}
>
  {(props) => (
    <div class="flex gap-4 items-center">
      <Avatar
        src="https://images.unsplash.com/photo-1534528741775-53994a69daeb?w=100&auto=format&fit=crop&q=80"
        alt={props.alt}
        text={props.text}
        size={props.size}
        badge="i-lucide:check"
        badgePosition="bottom-right"
      />
    </div>
  )}
</Playground>

## Features

- **Image Preloading & Status**: Preloads `src` with `Image` in the browser and exposes `idle`, `loading`, `loaded`, or `error` through `data-status` and `onStatusChange`.
- **Fallback Initials**: Uses explicit `text` when provided; otherwise derives up to two uppercase initials from `alt`.
- **Icon Fallback**: `fallback` can provide an icon instead of fallback text.
- **Status Badge**: Position a badge icon in any corner with `badgePosition`.
- **Accessible Fallback**: The loaded image uses `alt`; while unavailable, fallback content can use `alt` or `text` as its accessible label.

`Avatar` preloads the source before exposing it on the rendered `<img>`. It does not set native `loading="lazy"` automatically.

## Anatomy

```text
root (<span>)
├── image (<img>)
├── fallback (<span>)
│   └── fallbackIcon (optional)
└── badge (optional)
```

## Examples

### Single Avatar
<Example path="./single-avatar" />

### Sizes
<Example path="./sizes" />

### Fallback Modes
<Example path="./fallback-modes" />

### Badge Positions
<Example path="./badge-positions" />
