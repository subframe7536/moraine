---
title: Icon
description: Renders an icon from a UnoCSS icon class, JSX element, or render function.
sidebar:
  order: 2
search:
  tags: [Iconify, Lucide, symbol, SVG]
---

import { Icon } from '@src'

## Import

```tsx
import { Icon } from 'moraine'
```

## Playground

<Playground
  controls={[
    { kind: 'input', prop: 'size', label: 'Size (px)', defaultValue: 28, inputType: 'number' },
    { kind: 'input', prop: 'name', label: 'Icon identifier', defaultValue: 'i-lucide-sparkles' },
  ]}
>
  {(props) => {
    return (
      <div class="p-4 flex items-center justify-center">
        <Icon name={props.name} size={props.size} class="text-primary" />
      </div>
    )
  }}
</Playground>

## Slot Structure

Icon element with no slot.

## Examples

### Icon Sizes

Numeric pixel sizes from 12 to 48.

<Example path="./icon-sizes" />

### Icon Gallery

Common Lucide icons rendered via UnoCSS classes.

<Example path="./icon-gallery" />

### Icon as JSX

Pass a JSX element or render function instead of a string name.

<Example path="./icon-as-jsx" />
