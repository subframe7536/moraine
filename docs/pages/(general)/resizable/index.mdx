---
title: Resizable
description: A multi-pane layout component with explicit panels and handles.
sidebar:
  order: 12
search:
  tags: [panels, splitter, drag, resize, split view, resizable]
---

import { Resizable } from '@src'

## Import

```tsx
import { Resizable } from 'moraine'
import type { ResizableT } from 'moraine'
```

`Resizable.Panel` owns panel sizing and constraints. Place `Resizable.Handle` only where adjacent panels should interact; omitting it leaves the panels adjacent without a separator.

## Playground

<Playground
  controls={[{ kind: 'switch', prop: 'disable', label: 'Disabled', defaultValue: false }]}
>
  {(props) => (
    <div class="border border-border/60 rounded-xl bg-card/30 h-48 max-w-xl w-full overflow-hidden">
      <Resizable disable={props.disable}>
        <Resizable.Panel defaultSize="35%" min="20%" class="p-4 bg-muted/20">
          Sidebar Panel
        </Resizable.Panel>
        <Resizable.Handle />
        <Resizable.Panel defaultSize="65%" min="30%" class="p-4 bg-background/50">
          Main Editor Content
        </Resizable.Panel>
      </Resizable>
    </div>
  )}
</Playground>

## Anatomy

```text
Resizable
├── Resizable.Panel
├── Resizable.Handle
└── Resizable.Panel
```

Each handle can independently set `action="collapse"`, enable `intersection`, or render a custom grip through its children. Use `children={false}` to keep the divider while omitting the inner grip.

## Keyboard interaction

| Key                                          | Description                                                   |
| -------------------------------------------- | ------------------------------------------------------------- |
| <kbd>ArrowLeft</kbd> / <kbd>ArrowUp</kbd>    | Resizes in the decreasing direction for the active separator. |
| <kbd>ArrowRight</kbd> / <kbd>ArrowDown</kbd> | Resizes in the increasing direction for the active separator. |
| <kbd>Home</kbd>                              | Moves the adjacent panel toward its minimum permitted size.   |
| <kbd>End</kbd>                               | Moves the adjacent panel toward its maximum permitted size.   |
| <kbd>Enter</kbd>                             | Toggles the nearest collapsible panel for collapse handles.   |

## Examples

<Preview path="./constraints" />

<Preview path="./composition" />

<Preview path="./controlled" />

<Preview path="./collapsible" />

<Preview path="./vertical-disable" />

<Preview path="./nested-panels" />
