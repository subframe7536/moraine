---
title: Badge
description: Compact label component with icon slots and variant styles.
sidebar:
  order: 3
search:
  tags: [label, tag, status, metadata]
---

import { Badge } from '@src'

## Import

```tsx
import { Badge } from 'moraine'
```

## Playground

<Playground
  controls={[
    { kind: 'input', prop: 'title', label: 'Title', defaultValue: 'New feature' },
    {
      kind: 'select',
      prop: 'variant',
      label: 'Variant',
      defaultValue: 'default',
      options: [
        { label: 'Default', value: 'default' },
        { label: 'Outline', value: 'outline' },
        { label: 'Solid', value: 'solid' },
        { label: 'Subtle', value: 'subtle' },
      ],
    },
    {
      kind: 'select',
      prop: 'size',
      label: 'Size',
      defaultValue: 'md',
      options: [
        { label: 'Small', value: 'sm' },
        { label: 'Medium', value: 'md' },
        { label: 'Large', value: 'lg' },
      ],
    },
  ]}
>
  {(props) => {
    return (
      <Badge variant={props.variant} size={props.size} leading="i-lucide:sparkles">
        {props.title}
      </Badge>
    )
  }}
</Playground>

## Slot Structure

Compact label with optional leading and trailing icon slots.

```text
root
├── leading (Icon, optional)
├── label (optional)
└── trailing (Icon or IconButton, optional)
```

## Examples

### Variants

Default, outline, and solid styles.

<Example path="./variants" />

### Sizes

Size scale with leading and trailing icons.

<Example path="./sizes" />

### Status and Metadata

Common badge content for pills, counters, and status labels.

<Example path="./status-and-metadata" />
